# Changelog

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [未发布]

### Added
- 到点被跳过时，弹窗里说不出任何原因（`BACKLOG.md` A12 第 1 条，P3）。`decideAlarmAction` 判出
  `SKIP` 时是带 `reason` 回来的（`paused-all` / `auto-paused` / `user-active` / `discarded` 四种），
  执行器原先把理由丢掉直接返回。于是三种状态在用户眼里长得一模一样：倒计时归零、页面不动，
  分不清是"浏览器认为我在看这一页"还是"这个任务被暂停了"
  - 跳过那一拍现在把 `{reason, at}` 写进 `chrome.storage.session` 的 `rt:skip:<tabId>`：它每拍都在重写，
    寿命又不超过这次浏览器会话，正是 session 的语义（放 local 等于给每个刷新周期多开一笔磁盘写）。
    刷新成功即清掉，停任务时随其它 `rt:` 键一起清。弹窗每秒随倒计时读一次，只按在任务清单里的
    tabId 逐个点名读，一个任务都没有时一笔都不发
  - 四条理由全部采集，但只在"别处看不到"的两条上显示：`paused-all` 与 `auto-paused` 各自已经有角标
    和状态文字，再缀一句是重复。所以 chip 只把「你在操作」「休眠中」摆出来，压制判据单独住在
    `skipEntry`（全局暂停、或该任务 `autoPaused` 时不显示），采集与显示分家这件事由门禁钉住
  - 时间戳不进正文，只在 `title` 里（`fmtClock(at)`）。弹窗行内多一段"时分秒"会把 `task-sub` 挤到换行，
    400px 的版面没有这个余量；理由短标签中英各有一道宽度上限守卫（中文 ≤6 字、英文 ≤16 字）
  - 纪律 3：新增 7 个语言包键（中英各 180 键，齐平由 validate 守着）；存储只在 session 多一个
    `rt:skip:<tabId>` 前缀，常量 `SKIP_RT_PREFIX` 住在 `shared/config.js`，因为后台写、弹窗读，
    两头拼的是同一个键；默认值一个没动，权限没有新增（session 不需要额外授权）
  - 门禁：新增 `tests/tab-auto-refresh/skip-trace.test.mjs` 20 条。理由表不是手抄的——它真跑
    `decideAlarmAction` 把四种 `SKIP` 理由收集出来与 `ALARM_SKIP_REASONS` 求并集，少一条就红；
    弹窗侧 `syncSkipTraces` / `skipChipText` / `skipChipTitle` / `skipEntry` 切真实源码执行。
    22 处对照（19 红 + 3 反向绿）逐条记在文件末尾，其中两处是第一轮实跑为绿、把断言补硬之后才红

### Fixed
- 「尊重你的操作」在用户离开浏览器之后朝着反方向静默失效：人去了别的程序，任务反而**一次都不再刷新**。
  `isTabOnScreen` 原先把两种状态压成一种——"这个 SW 实例还没收到过任何焦点事件"（确实不知道焦点在哪）
  与"最后一个事件是 `WINDOW_ID_NONE`"（已知浏览器不在前台）。后者被当成"未知"，于是回退去问
  `chrome.windows.getLastFocused()`，而真实 Chrome 在浏览器没有前台窗口时**仍然返回最后聚焦的那个窗口**
  （只有窗口一个都不剩才 reject）。回退正好把 `tab.windowId` 取了回来，于是用户走开期间的每一次触发都判成
  "人正看着这页"、这一拍跳过、下一拍再来一遍。现在三态分开：已知不在前台直接放行去刷，只有真未知才补查一次
  - 影响面（纪律 3）：只有 `skipOnActivity` 开着的用户（含从没碰过、按默认开的），而且只朝"多刷"的方向修正
    ——改前是走开之后不刷，改后是走开之后照刷，人真在看着时照旧跳过。所有默认值一个都没动
  - 之前躲过了门禁：桩件把 `getLastFocused` 在"没有焦点窗口"时建模成抛错，等于把"焦点在别的应用里"当成
    "浏览器一个窗口都没有"。`alarm-gate.test.mjs` 那条"焦点在别的应用里时照刷"因此是**为错误的原因变绿**的，
    它原本的红→绿对照改的是乐观分支返回值，碰不到这条真实路径。桩件建模已改对，该用例换成三条分别打
    冷启动无窗口 / 冷启动有最后聚焦窗口 / 浏览器退到后台但窗口还在，对照重跑过
  - 真机验证只能手工做（把任务页摆在当前窗口前台、焦点切到别的应用、等 2~3 个周期要照常刷新），
    已记进 `BACKLOG.md` 的 V1 (c)
- cookie 备份会越界捞进无关站点的登录票据（`BACKLOG.md` A2，P0）。链条是两段拼起来的：
  `siteRoot` 靠一张手写的多级公共后缀清单（`co.uk`、`com.cn`…），清单外的多级后缀一律只切两段，
  于是 `shop.example.co.nz` 的"注册域"算成了 `co.nz`（`co.nz` / `com.ua` / `com.ru` / `co.id` /
  `com.ph` / `com.vn` 都不在表里）；而 `domainChain` 又是"一路切到只剩两段"，把 `co.nz` 也交出去查。
  `chrome.cookies.getAll({domain})` 的语义是"域等于它或它的子域"，一次查询就把浏览器里**所有** .co.nz
  站点的 cookie 收进来、去重、按票据得分截到 200 条，明文写进 `cookieBackup:shop.example.co.nz`；
  `restoreCookies` 再按每条自己的 `domain` 写回浏览器——等于每次重启都替无关站点复活一遍登录态。
  三道约束一起改：① 判据从"抄全清单"换成形状规则（末段是两字母国家/地区码、倒数第二段是品牌段就多切一段），
  一次覆盖整个 ccTLD × 品牌段组合；② `domainChain` 下探到注册域为止，绝不查注册域以上那层；
  ③ 整串本身就是公共后缀时 `siteRoot` 返回 `null`，备份与探针一律放弃这个站点（宁可丢登录态，用户重新登录就好）。
  形状规则偏保守：判据命中就多切一段，最坏结果是根域算窄（少覆盖几个兄弟子域的 SSO 票），不会算宽。
  同一根因顺带修掉的两处：`sessionProbe` 以根域为键，过去两个无关站点共用一份掉线状态、A 站掉线会冻结 B 站的备份写入；
  `reloadTab` 的同站判断也吃 `siteRoot`，过去 `other.co.nz` 被当成"和 `shop.example.co.nz` 同站"，
  用户误开的外链页会被原地刷新而不是导航回监控目标
  - 历史备份按**收敛**处理而不是整条清除（纪律 3）：`planBackupConvergence` 只剔掉落在注册域之外的条目，
    其余原样留着，`timestamp` 不动（它是"最后一次有效备份"的时间，30 天 TTL 照原样起效）。
    整条清除会让"重启后恢复登录"在用户没碰过任何开关的情况下静默失效。逐路径核对：键本身是公共后缀的
    （`siteRoot` 为 `null`）与筛完为空的整条删掉，前者里面每一条都属于别人；结构不成样子的条目不在这里处理，
    仍由 `pruneCookieBackups` 的 TTL 与 20 站额度淘汰。收敛排在恢复之前，反过来等于先复活再删存档
  - 默认值一个都没动：`cookieBackup` 仍默认关闭，判据只在"开启且站点在多级公共后缀之下"时才影响结果
  - 之前躲过了门禁：桩件的 `cookies.getAll` 恒返回 `[]`，备份采集与还原整段在空数据上跑（`BACKLOG.md` A6 第 8 条）。
    现在罐子按 `domain` 真过滤、`set` 会 upsert，并新增 `calls.cookieGet` 记录**查了哪几层域**——
    越界与否恰恰体现在查询面上，只记写入结果等于放过了它。纯函数层 4 条 + 执行器层 13 条新用例，
    五处红→绿对照实跑过（含两处第一次跑是绿的、补断言之后才红），完整红名单记在
    `tests/tab-auto-refresh/cookie-backup.test.mjs` 末尾
- 弹窗会按当前标签页的任务回填关键词、"命中后继续盯守"与实际间隔（`BACKLOG.md` A5）。
  形状不是"少显示一行"而是数据丢失入口：想改关键词只能停掉再重开，而重开那次读的是空输入框，
  原来那条监控就此静默消失——任务列表里的关键词 chip 照常显示（它读的是 `tasks`），所以看起来一切正常
  - 间隔回填排在 `initPresetSelect()` 之后：那一步按 `settings.lastIntervalSec`（"最近一次手动值"）预填，
    当前标签页真正在跑的间隔必须盖掉它，否则显示的是别的页面上一次用的数
  - 也刻意**不**挂到 `storage.onChanged` 的重绘回流上：弹窗活着期间 `currentTab` 是 init 里 query 出来、不再变化，
    而回流会反复发生，挂上去等于随时抹掉用户正在输入的字。早退条件（没有任务就一个字都不动）因此是契约的一部分
  - popup 此前没有任何门禁覆盖（DOM + `chrome.*` 的混合体，仓库不引第三方 DOM 库），所以这个修复原本只能真机验。
    补了一份按花括号配对切出 `populateTaskFields` 真实源码、把 `$` / `tasks` / `currentTab` 当形参注入执行的用例
    （`tests/tab-auto-refresh/popup-repopulate.test.mjs`，10 条），形状与 `keyword-inpage.test.mjs` 一致；
    其中"函数真的被 init 调用，且排在 `initPresetSelect()` 之后"那条守的是死入口与顺序。七处红→绿对照实跑过。
    真机仍要验一次：控件确实被填上、以及"先停后起"不丢关键词，这两点切片用例给不了（`BACKLOG.md` V1 (d)）
- `settings` 的两条"读基座 → 整份写回"链互相覆盖（`BACKLOG.md` A7）。点"开始"记间隔的 `rememberLastInterval`
  与切开关的 `save-settings` 各自 `getSettings()` → `Object.assign` → `sync.set({ settings: 整份 })`
  （`tasks` 有 `withTaskLock`，`settings` 一直没有），交错时后落地的把先落地那笔按旧值写回去——
  表现为"我明明勾了，它又自己弹回去"，而且 sync 会把这份错的合并结果漫游到其它设备。
  `invalidateSettings()` 只保证读到最新落盘值，管不了两个读-改-写之间的交错，所以必须串行
  - 两条链收进同一个 `patchSettings`，由新增的 `withSettingsLock` 串行（与 `withTaskLock` 同形状、各自独立的一把锁）。
    读写两头各失效一次，各管一件事：读之前不失效就会拿过期快照当合并基座（`onChanged` 回流有延迟，
    那个窗口真实存在）；写之后不失效则回流前的一切读取仍是写前的值——弹窗"发送测试消息"await 到写盘完成
    才发出，靠的正是这一次（2.0.0 修过的时序竞态，不能被 2.1.0 的快照重新引入）
  - 「间隔没变就不写」的判断挪进锁内，与写盘同处一把锁：在外面先读再判，读到的可能不是落盘时的基座，
    等于把这条链又变回无锁读-改-写。`partial` 给函数时返回 `null` 即"这次不用写"
  - 锁的方向是契约：`startTask` 在任务锁内 `await` 设置写盘，那是单向等待；反过来（设置锁内再排任务锁）
    两条锁互相等死，弹窗表现为一直卡住。`loadSettings` 的 local→sync 迁移是**唯一**不经 `patchSettings`
    的写点，因为它在 `getSettings` 的调用栈里，改成走它就是自己等自己
  - 门禁：`settings-cache.test.mjs` 新增四条——两个并发的 `save-settings` 不互相覆盖（锁本身）、
    `onChanged` 还没回流时不许拿内存快照当基座（读前失效）、手动起任务与切开关并发谁都不许把对方等死
    （锁的方向）、间隔没变就不写盘且这条判断仍在锁内。六处红→绿对照实跑过，每处恰好红一条。
    两处第一次不合格并记在文件末尾：去读前失效那处当时**没有任何用例判得到**（唯一的失效用例喂的是
    回流已到达的情况，补了"还没回流"那条才红），反向成环那处前两版一个跑绿（变异与 `startTask`
    是同一等待方向，不成环）、一个把整份 suite 挂住（无条件插环会连单向那条一起吊死，而跑手只等退出码，
    于是加了 90 秒超时并把挂住单独报出来）
  - 纪律 3：默认值、存储键、存盘数据结构一个都没动。受影响路径只有上面两条写盘入口，
    老用户已有的 `lastIntervalSec` 与其它开关从此不再被并发写丢掉
- `runtime.onMessage` 的分发口开始认来源，`save-settings` 的载荷按已知键收（`BACKLOG.md` A9）。
  MV3 下普通网页到不了这个入口，所以这不是"任意网页能调扩展 API"的洞；真实存在的来源是**我们自己注入到
  被监控页里的** `content/keepalive.js`——它带的就是 `sender.tab`，而分发口原先全程不看 `sender`，
  任何一条分支它都能走：起停任务、整份改写设置、触发外发。现在守卫排在异步分发体的第一行，
  页面侧只放行 `keepalive-query` 与 `user-activity` 这一对（注入脚本实际会发的全部），其余回 `ok: false`
  - 反向刻意不守：弹窗发 `user-activity` 自己就空转（它只认 `sender.tab.id`，不看 msg 里的 id），
    `keepalive-query` 是只读的配置快照，拒了只会给调试添乱
  - **新增页面侧消息类型必须同时登记进 `FROM_PAGE_TYPES`**。漏登记的表现很静默：注入脚本照发，
    守卫照拒，而 `keepalive.js` 那边的失败是自己吞掉的，功能当场失效却不报错。所以有一条用例去扫
    `keepalive.js` 真实源码，把它发出的 `type` 与白名单逐条比对
  - `save-settings` 的增量现在过 `pickKnownSettings`（`shared/logic.js` 的纯函数）：只留
    `DEFAULT_SETTINGS` 里有的键，其余整条丢弃。放任意外来键进 `settings` 的代价是持久的——它会跟着
    `sync` 漫游到用户其它设备、占 8KB 配额，而代码里没有任何一处读它。判据必须是**自有属性**而不是 `in`：
    `constructor in DEFAULT_SETTINGS` 为真，用 `in` 等于把整条原型链上的键都当成合法设置键收下
  - 只管键不管值：值的形状本来就由读侧负责（`normalizeStoredSettings` 补默认，`webhookUrl` 另有
    `normalizeWebhookUrl` 把非法值当空处理），在这里重复一遍只会多一处会漂移的判断
  - 纪律 3：默认值一个都没动，正常路径（弹窗勾选保存、注入脚本拉配置与上报活动）行为不变。
    唯一收窄的是"载荷里的未知键不再进盘"，而弹窗自己从不发未知键；已存在老存盘里的多余键仍按原样
    被读侧忽略，`webhookEvents`（1.7.0 旧名）这类改名遗留也不会被这次写盘复活
  - 门禁：新增 `tests/tab-auto-refresh/message-gate.test.mjs`（9 条）——八条弹窗专用类型逐条从页面来源
    发一遍并要求"拒绝且零副作用"（按存储、alarm、reload、外发、通知、角标全量取指纹比对），合法两种
    照常用，`user-activity` 不许冒充别的标签页，白名单端到端与纯函数各一条，再加两条扫源码
    （守卫必须在分发链最前面、白名单必须与 `keepalive.js` 实际发的类型齐平）。八处红→绿对照实跑过，
    每处的红名单原样记在该文件末尾，其中"守卫挪到第一条分支之后"那处正是只有源码扫描抓得到：
    行为上八条里只有一条逃守卫，循环里其余七条照旧绿
- 关键词与验证墙都只查顶层框架，子框架里的墙与正文一律检不到（`BACKLOG.md` A3，P1）。
  两处 `executeScript` 都只给 `target: { tabId }`、没有 `allFrames`，而本插件常驻 `<all_urls>` 主机权限，
  跨源 iframe 一样能注入——检不到纯粹是没要，不是做不到。代价不对称地落在验证墙那侧：
  Cloudflare 那类整页挑战常常就嵌在一层 iframe 里，顶层文档只剩一个空壳标题，于是"验证墙连续命中就暂停"
  这道守卫对这类页面形同虚设（任务对着墙空刷，正是它本该拦住的情形）；关键词侧则是页内框架里的正文
  （嵌入式商品组件、iframe 里的库存状态）永远命中不了
  - 两处注入收进同一个 `executeInAllFrames`。**加多框架不许把原来能成的场景换成新的失败**：
    `allFrames` 的失败方式是整次调用 reject（一个够不着的沙箱框架、`view-source` 就能带走整页结果），
    所以拒了之后退回 `frameIds: [0]` 再试一次，两次都失败才交给调用方按"注入失败"处理
  - 关键词侧用 `aggregateFrameHits` 合并各框架命中集，按"取到几个算几个"：一个都没取到才回 `null`
    （那条提前结束的分支语义没变）。刻意区分"注入了、确实没命中"（回 `[]`，继续 3s/10s 重采样）与
    "根本没注进去"（回 `null`，等下个刷新周期）——合并时把它们混成一种，注入失败就会被当成没命中而空转
  - 验证墙侧是纪律 1 的又一次兑现：注入体 `captchaProbe` 只回**原始事实**（是否顶层、标题、自身网址、
    视口宽高、挂在文档里的 iframe/frame/script 网址），两条正则一个都不下页面，判定全在
    `decideWallFromFrames`。这与 `matchInPage` 那份"必须自包含所以只能重复实现、靠门禁比对钉住"的情形相反——
    这里能不分叉就一份实现，于是页内没有判断，也就没有第二份会漂移的判断
  - 子框架多过一道视口地板（400×250）：常见挂件尺寸都在它之下（reCAPTCHA 复选框 304×78、
    Turnstile 300×65、hCaptcha 300×88，隐藏的框架 0×0），而整页挑战的 iframe 是视口尺寸。
    地板只加在子框架这一侧，理由还是那个不对称：漏判只是少一次暂停、下轮刷新还会再看，误判会让任务
    卡在暂停态且不自愈。**顶层判据逐条维持原样**（含"顶层文档挂着 reCAPTCHA 资产就算墙"这条偏严的旧行为），
    要不要把它一起收紧是另一个决定，得连着改默认语义，不在这一条里顺手做
  - 载荷上界：`allFrames` 会让每个框架都回一份，所以资产列表封顶 50 条、每条网址与标题各自截 300
  - 纪律 3：默认值、存储键、阈值一个都没动。受影响路径只有上面两条检测链，方向单一——原来检不到的现在检得到；
    用户显式设过的 `captchaGuard`、关键词与 `onHit` 语义都不变。刻意**没**跟着扩到全部框架的一处是
    保活心跳脚本注入（仍是顶层）：把同一份模拟活动注进每个子框架会向对方服务器放大请求量，而子框架的
    `document.hidden` 语义与顶层不同，那是另一件事。权限没有新增（`scripting` 与 `<all_urls>` 本来就有）
  - 门禁：新增 `tests/tab-auto-refresh/frame-scan.test.mjs`（27 条，四层：多框架聚合与逐框架判定的纯函数层、
    按花括号配对切出 `captchaProbe` 真实源码执行的注入体层、真跑注入体再喂给判定的两层接缝、
    走共享桩件的执行器接线）。`detect-chain.test.mjs` 的验证墙桩件跟着改成事实对象形状。
    十处红→绿对照实跑过，每处只红在该红的那几条（含"改前源码"整文件加载失败）；第 10 处
    （注入体丢掉自身网址）**第一版跑出来是全绿**——当时 `url` 那一支在整个测试面上没人单独依赖过，
    所有子框架样本都同时带了指纹标题；补了两条只靠网址命中的用例之后才红。逐条红名单记在该文件末尾
- 外发载荷带着被监控页面的完整网址，query 里的一次性令牌随之发给第三方（`BACKLOG.md` A4，P1）。
  三个外发点各有 `url:` 字段——关键词命中、任务自动停止、任务自动暂停，填的都是原始 `task.url`，
  而工单与后台系统这类页面的 query 常带签名、会话令牌、邮箱手机号。这些字节原本会原样 POST 给用户配的
  webhook（Discord / Slack / Telegram / ntfy 的服务器，链路上一切可见方），微信卡片跳转用的也是同一份。
  现在只发 `origin + pathname`；`session-lost` 本来只有 `host`，不在其列
  - 剪的位置是 `notifyOut` 这个总入口，不是三个调用点：两个出口读的是同一份 `payload.url`，
    逐处改必然出现"改了 webhook 忘了微信"（对照第 2 处就是照这个形状改的，既有那 11 条外发用例
    **一条都不红**），而放在总入口将来新增事件也不必记得重复一遍
  - 新增 `shared/logic.js` 的 `outboundUrl`，与 `urlKey` 形状相同却刻意不共用：`urlKey` 是页面认领的
    匹配键，为了匹配它将来放宽（忽略尾斜杠、把 hash 算进去）都合理，而那类改动对匹配是修 bug、
    对外发是漏令牌。解析不出来的网址当"没有网址可发"（回空串），绝不回退成原样传出去
  - 已定：不加"是否带完整网址"的开关，直接切。精简版仍然指得清是哪个站点的哪条路径，
    而留一个"带 query"选项等于长期保留一个把令牌外发的入口（深链落点的代价见下条）
  - 纪律 3：默认值、存储键、存盘数据结构一个都没动，盘上什么都没变；`task.url` 本身照旧存全串
    （刷新、重开、页面认领都要用它），只有离开本机的那份被剪。朝用户可见的方向要说清两条代价：
    ① 解析整页 `url` 字段的 webhook 接收端从此拿不到 query（`host` 字段没动）；
    ② 卡片跳转链接同样不带 query，而对 **hash 路由的单页应用**（`https://app/#/board?id=7` 这类，
    视图信息整个活在 `#` 后面）落点会从"那个视图"变成"应用根"。这是把令牌挡在外发面的定价，
    已定不加开关；真机要验这一条，记在 `BACKLOG.md` V1 (g)。关键词命中载荷里的 `text` 是用户自己填的
    监控词，这是"我让你盯这几个字"的本体，**保留**不外发就等于功能不存在，README 外发一节现已点名
  - 顺带两条同一条目的账：`webhookUrl` 自己也是凭据（带 token 的地址拿到就能往里发），
    README 原先只说了微信凭据同步、没提这一条，已补；密钥输入框从 `type="text"` 改成 `password`
    （只挡屏幕旁边的眼睛，明文存 sync 这件事没变，掩码也不改变已存值）
  - 门禁：`outbound.test.mjs` 里通用的任务网址换成带一次性令牌的形状，于是那 11 条断言外发形状的用例
    顺带都在检查剪没剪；另加四个事件各一条端到端用例（两个出口同时开，扫的是序列化之后的原始请求字节，
    含"载荷里不许出现这串令牌/邮箱"与"剪过之后网址必须还在"两侧）。`logic.test.mjs` 加 `outboundUrl`
    一条，`popup-repopulate.test.mjs` 加一条扫 `popup.html` 的源码守卫（并新增 `TAR_POPUP_HTML` 重定向入口，
    入口本身也实跑验过）。七处红→绿对照逐条红名单记在 `outbound.test.mjs` 与 `popup-repopulate.test.mjs` 末尾
- webhook 外发失败完全无痕，配好地址却什么都没收到时没有任何一处能看（`BACKLOG.md` A8，P2）。
  改前的 `postWebhook` 把 fetch 发出去就不管了：连状态码都不读，非 2xx 与网络失败一起落进一个
  只写着"失败静默"的空 catch，成功与失败在用户侧完全同形。而微信侧从 2.0.0 起就有
  `wechatLastResult` 留痕与「发送测试」按钮，两个出口能力不对等——同一个"我配好了到底通不通"的问题，
  微信问得出、webhook 问不出。现在两头同构：投递结果写 `chrome.storage.local` 的 `webhookLastResult`
  （`{ok, kind, status, errorKey, event, at}`，只留最近一次），弹窗显示状态行并有「发送测试」
  - 状态码分类是 `shared/logic.js` 的纯函数 `webhookResultOf`（纪律 1）：执行器只负责把 HTTP 状态与
    正文取回来交给它。分四桶加两个兜底（401/403 地址失效、404/410 接收端不认、400/406/413/415/422
    载荷不合、429 被限流，其余 5xx 归服务端、剩下的归"其它状态码"），错误码再翻成"该去哪改"的一句话——
    与 `wechatErrorKey` 同一路数，给用户的不该是一个数字
  - 2xx 这一路还要读正文：Slack 那类接收端对"hook 已删除 / 频道被踢"照样回 200，失败信号只在正文里的
    `ok: false`。只看 `res.ok` 会把这类记成成功，而那正是本条要修的形状。刻意**不**去认它们的纯文本
    错误（`no_service` 那一类）：判据一开始按字面猜，绿和红就都不再可信，宁可显示"200 成功"也不凭猜把
    正常投递报成失败。正文截前 2000 字再判，代价（标记埋在窗口之外就认不出来）钉在 `logic.test.mjs`
  - 两种"本该不发"的早退**刻意不留痕**：地址非法（那是配置态，弹窗另有红字）与事件没勾上
    （用户明确没订阅，写一条"失败"反而是噪音）。这一条是显式决定，由用例钉住
  - `webhook-test` 与 `wechat-test` 同语义：只要求地址合法，不受事件勾选约束，也不惊动微信那个开关。
    弹窗侧新增 `#webhookRow` 一行（原先独占一行的地址非法红字并了进来），沿用微信那条 `.wx-row` 的
    显隐套路——没填地址就整行 `hidden`，不占高度
  - 纪律 3：默认值一个都没动，新增的是一个 `local` 键而不是 `settings` 键，所以不随 sync 漫游、
    也不占那 8KB 配额。没填过地址的用户（含全部老用户默认态）看不到任何变化；填过的用户开弹窗先看到
    "已开启，等待首次推送"，直到下一次真投递或点一次「发送测试」才变绿/变红。权限没有新增（纪律 2 无账要记）
  - 弹窗高度是有预算的（Chrome 弹窗外框上限 600px，设置卡片本就贴着上限）：这一行只在填了地址时出现，
    多约 10px。本机没有 playwright，量不了，真机要验"填地址之后弹窗不出现滚动条"，记在 `BACKLOG.md` V1 (h)
  - 门禁：`outbound.test.mjs` 加 10 条（留痕形状与覆盖、200 里的拒绝、归桶表逐码、非 2xx 不信正文、
    网络层、"本该不发不留痕"、两出口互不干扰、「发送测试」绕过事件清单且不动微信留痕），
    `logic.test.mjs` 加 5 条纯函数用例（含手写表与 `WEBHOOK_STATUS_BUCKETS` 的反向同源核对），
    `popup-repopulate.test.mjs` 加 10 条（切 `renderWebhook` 真实源码跑，另扫 `popup.html` 的控件齐备与
    `popup.css` 的颜色类），`message-gate.test.mjs` 加一条与 A9 那条相反的守卫：分发链里每条弹窗专用分支
    都必须登记在 `POPUP_ONLY`——新加分支忘了登记时"每条都拒"那条永远不会红，因为它遍历的就是那张表。
    23 处红→绿对照逐条红名单记在这四个文件末尾；其中两处**第一次跑是绿的**（假 DOM 初值太干净、
    失败留痕少喂一个 `status`），补了输入形状才红，教训与 A5 处 4 是同一条
- 微信教程与弹窗文案自己打自己的脸（`BACKLOG.md` A10，P3）。三处，都是照着做的人看得见的：
  ① `wechatIntro` 说测试号页"可一次拿到下面四项"、`guideStep1Title` 说"拿到四项凭据"、
     `wechatGuideHint` 的步骤串说"抄四项凭据 → 建模板"，而同一个向导里的 `guideStep1Body2`
     写的是"复制三项"。真实情况是页面上只有 appID、appsecret、openid 三项，第四项（模板 ID）
     是第二步建完测试模板才生成的——顺序也正好被那串步骤写反。中英两份一起统一到
     "页面抄三项 + 建模板得第四项"，第二步的标题跟着点名它产出第四项，让 3+1=4 这笔账在读者眼里对得上
  ② `wechatErrTemplate` 让用户"把模板正文写成 `{{title.DATA}}` 与 `{{content.DATA}}` 两行"，
     给出的正是教程里明确说会被平台整行丢弃的那种裸变量写法。也就是说：模板已经写对了的用户，
     照着这条错误提示去"修"，会亲手把它改坏。已补上「关键词 + 中文冒号」前缀，与 `wechatTplBody` 同口径
  ③ `guideFixTemplate` 结尾写着"若卡片仍是空白，把中文冒号换成英文 : 再试一次"，与同页
     `guideStep2Why` 的硬规矩（微信只认「关键词：{{变量}}」）直接相反。这条兜底在本仓库没有任何实测或
     文档依据，而按它做等于把唯一合规的写法改掉——删掉，改成"平台丢整行时不返回错误码，逐字比对这两行"
  - 说清楚没验到的部分（免得后一轮把它当已证事实）：中文冒号是必需还是仅仅充分，本仓库没有实测过，
    依据只有 2.0.0 当时的调查记录加官方文档示例的写法。这一条修的是"两处口径打架"，取的是"教程只教一种"，
    没有断言英文冒号一定不行。手边有微信测试号时值得真机验一次，记在 `BACKLOG.md` V1 (i)
  - 纪律 3：改的全是 `message` 文本，键一个没增没删（validate 的键齐平守卫照常过），不新增设置、
    不动默认值、不动存储。受影响的用户可见面只有微信配置二级视图与教程页的文字；`wechatIntro`
    中英各长了不到一行，弹窗那 600px 预算要真机看，V1 (h) 的范围因此扩到微信配置视图。权限没有新增
  - 门禁：新增 `tests/tab-auto-refresh/wechat-copy.test.mjs` 12 条，同时是原先
    `_code-review/verify-wechat-template-doc.mjs` 的入库替身——那份不入库，新 clone 里没有它，
    示范跑偏无人拦。三条规则的数都不写死：凭据数从 `DEFAULT_SETTINGS` 的 `wechat*` 字段数现推、
    模板变量名从 `WECHAT_TEMPLATE_KEYS` 现推、给用户抄的示范从 `wechat-setup.html` 的 `<pre>` 现取，
    代码与文案谁先跑偏谁变红。另有两条反空跑守卫（扫到的文案条数下限、提到英文冒号的子句数下限）。
    18 处红→绿对照：17 处按预期变红，第 18 处是反向对照（改一条守卫不该管的文案，必须仍绿）——
    没有它，"动一个字就红"的守卫同样能拿满分。其中 K10 **第一次跑是绿的**：子句只按中文句读切，
    整段英文算一句，前一句里的 `do not omit` 替后一句的兜底建议作了证；把英文句点也算边界之后才红。
    逐条红名单记在该文件末尾
- 弹窗的五个文本框只在失焦时保存，"打完字直接点弹窗外"这一次输入整条丢掉（`BACKLOG.md` A11，P3）。
  绑的只有 `change`，而 Chrome 弹窗一失去焦点就整体销毁——文档都没了，`change` 自然不会触发。
  webhook 地址与微信凭据四项全是"填完不点别处就关窗"的那种输入，重开弹窗是空的，用户只会认为它没记住。
  同一处绑定还压着另一半：`renderWebhook()` 也挂在 `change` 上，于是 A8 那条"地址非法给红字"
  在真实操作中从来没出现过——它要等一次失焦，而失焦通常就意味着关窗
  - 现在是四档：`input` 去抖 500ms 写盘并同步重算状态行（红字一边打一边出现）、
    `change`（回车、失焦）立刻写、两个「发送测试」先 `await saveNow()` 再发、
    `visibilitychange → hidden` 与 `pagehide` 各补一次
  - 刻意**不**改成逐字符立即写：一笔 `sync.set` 会回流成 `storage.onChanged`，弹窗于是每敲一个字
    重读一遍存储、整体重绘一次，后台那份 settings 快照也跟着每次失效；`chrome.storage.sync` 那族
    每分钟写次数上限还在后面。具体阈值没写进注释，也没写在这里——本仓库没实测过它
  - 关窗那一笔是补救不是保证：文档正在销毁，`sendMessage` 来不来得及都不一定。
    "打完字 0.5 秒内点弹窗外"仍只能真机验，记在 `BACKLOG.md` V1 (e)
  - 微信的「发送测试」补上 `try/finally`（webhook 那条从 A8 起就有）：中途任何一次 await 抛错
    （SW 正好被回收就是这种时候）原先会把按钮永久留在 disabled +"发送中"，错误本身被吞成
    "什么都没发生"。两条按钮现在同形，连"先落盘再测"的时序也同形
  - 纪律 3：默认值、存储键、消息类型一个都没动，改的全是弹窗侧的事件绑定与写盘时机；
    新增的 `SAVE_DEBOUNCE_MS` 与 `TEXT_SETTING_INPUT_IDS` 都住在 popup.js，不入存储。
    权限没有新增（纪律 2 无账要记）
  - 门禁：新增 `tests/tab-auto-refresh/popup-save-timing.test.mjs` 16 条，把 `scheduleSave` /
    `saveNow` / `flushPendingSave` / `cancelScheduledSave` 切真实源码、接假时钟与写盘计数器真跑，
    所以"敲五次只落一笔""`saveNow` 之后到点不再写第二笔""没有待写时 flush 一笔都不写"
    是跑出来的断言，不是读代码读出来的。另有一条反-drift：以后谁在 `saveSettings` 里多读一个
    清单外的文本框，它就悄悄退回"只有失焦才保存"而没人想起来——那条会直接变红。
    19 处红→绿对照（17 红 + 2 反向绿）逐条记在文件末尾；其中一处开发期撞中 `sel.addEventListener`
    的 substring 坑（"sel." 的尾巴是 "el."），另一处是本条的连锁：`renderWebhook()` 的调用点
    从 4 处变 5 处，`popup-repopulate.test.mjs` 的计数守卫跟着改
- 站点把"会话过期"表现成**同主机**跳到 `/login` 时，掉线检测自己把自己关掉了（`BACKLOG.md` A14，P1）。
  `task.url` 原先身兼两义——既是"用户指定的监控对象"，又是"这一页此刻的地址"，于是出现自指：
  第一拍 `reportSessionSignal` 记 `sus:1`，紧接着 `refreshTaskUrl` 因 `sameHost` 成立把 `task.url`
  改写成 `…/login`；第二拍起 `!looksLikeLoginPage(task.url)` 恒假 → 信号永远报"正常" → `sus` 被清零。
  确认窗口要 2 次采样，这条通道因此**永远走不到 `lost`**：没有通知、角标不红。后果不止一半——
  监控目标从此就是那张登录页，关键词在登录页正文里找、心跳对着 `/login` 发、用户真登录成功之后
  也不会自动回到原页面。而 `httpHeartbeat` 关着的用户只剩这一条通道，等于完全没有掉线检测
  - 语义定死一次：`task.url` 是**用户指定的监控对象**，不是当前地址。登录页不参与改写，
    判据抽成 `shared/logic.js` 的纯函数 `shouldAdoptTaskUrl(taskUrl, tabUrl)`（纪律 1 的兑现处），
    `refreshTaskUrl` 只剩执行。没有另加"用户填的原始目标"字段：多一个键就多一处要跟 `prune`
    的挂接/重映射/补记逻辑对齐的地方，而"永不完全等于登录页"这一条就已经堵死自指
  - 原意没被砍掉：登录完成后地址离开登录页，那一刻照常更新成真实目标页，"自动重开要打开实际页面"
    这条通路完好，由两条反向门禁盯着（改坏成"任何情况都不跟随"会当场红 2 条）
  - 纪律 3（已受损记录）：被旧版本改写成 `/login` 的任务，**不需要迁移也会自愈**——用户登录成功、
    页面离开登录路径的那一刻就跟随更新回业务页（这条有专门用例）。残余边界：用户从此不再登录，
    任务就一直监控着那张登录页，行为通道对它照旧豁免（这是"用户故意监控登录页"该有的语义，
    不为它加特例）。除此之外默认值、存储键、消息类型、权限全都没动
  - 之前躲过了门禁：`heartbeat.test.mjs` 只驱动 fetch 那一侧；`notifications.test.mjs` 那条行为通道用例
    给的是"登录页地址由 `changeInfo.url` 带、标签页本身仍停在业务页"的形状，恰好绕开"自己把自己改写"
    这一步。真实现场是页面**停在** `/login`、事件不带 url，后台只能从 `tabs.get` 读到
  - 门禁：新增 `tests/tab-auto-refresh/task-url.test.mjs` 4 条（真实现场的端到端确认窗口、
    单拍疑似要留得住、登录后回到业务页、同站跟随与跨站不覆盖），`logic.test.mjs` 加 1 条纯函数表。
    7 处对照在仓库外整份副本上实跑（副本里 `tests/` 与 `tab-auto-refresh/` 同级，纯函数与 e2e 共用
    同一份被改坏的源码，一次改坏两头都看得见）：改回 A14 之前的形状红 2、去掉同站约束红 2、
    去掉"没变就不写盘"红 1（只有纯函数那头，桩件的 `set` 是合并写，e2e 无感）、畸形网址放行红 1、
    改过头成"永不跟随"红 2、确认窗口降成 1 次红 2、以及一处不在这条路上的对照只红在既有用例上。
    逐条红名单记在 `task-url.test.mjs` 末尾
- 一次「开始」把整把任务锁压在网络上：任务已经落盘、两条闹钟一条都没建，其它标签页的起停全排在后面
  （`BACKLOG.md` A15，P1）。`startTask` 原先的顺序是 `setTasks` → `backupCookies` → `armRefresh` → …
  → `ensureHeartbeat`，而 `backupCookies` 一旦这一拍确认掉线就会一路 `await` 到 `notifyOut` 的两笔 fetch
  （webhook 加微信，15 秒超时，微信还要先取一次 `stable_token`）。锁是全站共享的 `withTaskLock`，于是
  最坏几十秒里：弹窗按钮卡在"开始"上，别的标签页连「停止」都要排队。后果更硬的是另一半——
  `tasks` 已经写上而 `refresh-<id>` / `hb-<id>` 还没挂，SW 恰好在等网络时被回收就没人回来补挂，
  这就是 `AGENTS.md` 里那句"任务在、永不刷新"的僵尸的一个成因
  - 改法是两处顺序：备份那一拍整体挪出任务锁、排在整条锁内流程之后（`updateBadge` 与
    `rememberLastInterval` 后面）；`armRefresh` 与 `ensureHeartbeat` 提到紧跟 `setTasks`，
    中间不夹任何可失败的等待。**那笔外发仍然 `await`，没有改成裸甩**——裸甩会被 SW 回收截断，
    丢的正是"会话掉线"这条通知，`AGENTS.md` 外发一节本来就写着调用方必须 await，这一条由对照 N3 钉着
  - 与 `prune` 那条"先写盘再挂心跳"是同一条纪律的两半：`ensureHeartbeat(新 id)` 按**落盘后的键**读任务，
    排在写盘前面会读到"这个 id 没任务"而把心跳清掉；反过来写盘之后立刻挂表，是为了让"任务已存在"与
    "任务会刷新"之间不留可被回收的空档
  - 纪律 1：这条没有"先读后写、跨异步步骤共享状态"的判据要抽——要改的是执行顺序本身，不是顺序里的决策，
    `startTask` 里该判的东西（受限页、拿不到标签页）一个都没动，所以没有新增纯函数，顺序由门禁钉住
  - 纪律 3（默认值与受影响路径）：默认值、存储键、消息类型、权限全未动，`startTask` 返回值仍是 `{safe}`，
    `r.safe` 的调用点照旧。可观察的顺序差只有两处：那笔首次备份现在晚于 `updateBadge()`（角标与
    `chrome.power` 锁因此提前收敛，原先它俩也压在同一等网络后面），以及 `startTask` 抛错时备份不再执行
    （原先同样不执行——校验排在备份之前，这条不是新行为）。老用户不受影响：改的是同一次「开始」内部的
    先后，不改变任何一拍的最终状态
  - 之前躲过了门禁：桩件的 `fetch` 从不返回 Promise 型应答（`BACKLOG.md` E3），`outbound.test.mjs` 判的是
    请求形状与失败留痕，不看"锁是不是压在网络上面"；而所有既有的 `startTask` 用例都是"等它 resolve 之后
    看结果"，一旦挂住，测试超时而不是变红
  - 门禁：新增 `tests/tab-auto-refresh/start-task-lock.test.mjs` 4 条。E3 那条限制在单条用例里绕开——
    `bootBackground` 之后把 `globalThis.fetch` 换成永不 resolve 的桩（顺序不能反，桩件安装时会替换它），
    并先用哨兵确认"确实挂在了网络上"再往下断言。四条判据：外发挂住时另一个标签页的「开始」照样完成、
    任务落盘之后紧接着就有两条闹钟、备份挪出锁之后那一拍没丢（fetch 恰好一笔、好样本没被坏样本覆盖、
    `session-lost` 通知照发）、建任务失败时不备份也不外发。6 处对照在仓库外整份副本上实跑：
    退回 A15 之前的形状红 2、备份挪到锁**之前**无条件执行红 3、改成裸甩红 1、心跳排到写盘前面红 2
    （连带既有的「右键开始 1 分钟」）、先挂表再验现场红 1；另有一处**零红**顺手量出来记成新账 A20
    （删掉 `stopTask` 里的 `stopKeepAlive` 全套一条都不红）。逐条红名单记在文件末尾

### Changed
- 共享测试桩件 `tests/helpers/background-harness.mjs` 按真实 Chrome 语义补齐十一处，为的是让"看着绿、实际空跑"
  的门禁能真的变红：`storage.set` 记写入键列表（`set` 是 `Object.assign` 合并，只比落盘后的内容比不出"没写"）；
  `alarms.getAll` 带出 `scheduledTime`（`chrome.idle` 回到 active 那段整个是按它判"已过期"的，此前在测试里永远走"没过期"分支）；
  `executeScript` 记下每次调用的 opts 并给出 `env.onScript` 钩子（原先恒返回"注入成功、没命中"，
  关键词命中后要做的三件事与验证墙连续命中的自动暂停因此一次也没被执行过，而桩件留的那个 `opts.__fixture`
  入口后台根本不读，从写下起就没人用得起）；`env.send()` 在监听器没 `return true` 时直接 reject
  （异步应答契约此前没有任何东西钉着）；`tabs.query` 真按 `active` / `discarded` / `windowId` / `currentWindow` 过滤；
  右键菜单有了登记表与 `env.fire.menuClicked`；cookie 换成真按 `domain` 过滤、`set` 会 upsert 的罐子，
  并用 `calls.cookieGet` 记下**查了哪几层域**（备份越界与否恰恰体现在查询面上，只记写入结果等于放过了它）；
  `windows.getLastFocused` 改成"只有完全没有窗口才 reject"（见上条 Fixed）；`env.fire.tabRemoved`
  先摘页再派发（真实时序，并记了一句"目前没有断言依赖它"）；`alarms.clear` 集中记日志；
  外加 `fetch` 桩件与 `env.reply()`（真实 MV3 SW 里 `fetch` 一定在，桩件没有它，三条外发链路一进 `fetch`
  就是 ReferenceError 又被各自的 `catch` 吞成"静默失败"——而静默失败正是它们唯一的失败方式，所以从没红过也从没绿过。
  桩件按规矩来：`ok` 由 `status` 推出而不是恒真、`url` 给的是**跟随重定向之后**的最终地址（掉线判据用的就是它）、
  放一个 `Error` 进去即 reject、给数组则按次序逐次应答。已知缺口写进注释：`init.signal` 只记录不兑现，
  "15 秒到点 abort"那条仍然没建模）
- 新增 `tests/tab-auto-refresh/detect-chain.test.mjs`（8 条）：关键词命中链与验证墙探测第一次端到端跑通
  （命中即停、继续盯守回写 `notifiedKeys`、已通知不重发、墙连续 3 次才暂停、墙消失自动解除、
  `captchaGuard` 关时不注入）。六处红→绿对照实跑过，其中包括一条反例：把桩件默认值改掉不会让任何用例变红，
  所以"命中"是靠用例自己造的，不是靠桩件的默认值
- 新增 `tests/tab-auto-refresh/tab-removed.test.mjs`（5 条）：会话中途关掉一张被监控的页会发生什么，
  此前 `fire.tabRemoved` 是个没人调用的死入口。五处对照每处只红一条，其中"create 回来一个没有 id 的对象"
  那一轮是补上去的——只留抛错那一轮时，删掉 `typeof newTab.id !== "number"` 那道判断照样全绿
- 新增 `tests/tab-auto-refresh/idle-resync.test.mjs`（5 条）：睡眠唤醒之后的闹钟自愈（过期的刷新闹钟重走
  完整周期、过期的心跳闹钟打散 0~60 秒重建、没主的不重挂只清掉）。这段此前是**看着在测、实际零覆盖**：
  判"已过期"靠 `scheduledTime`，而桩件不给这个字段，于是所有驱动 `fire.idle` 的用例只会走"没过期"分支。
  六处对照实跑过；其中"心跳打散在 0~60 秒"那处第一次改坏时**没有变红**（`Math.random` 换成常量 0 仍落在区间内），
  于是改成喂两个确定的随机值把 0 秒与 60 秒两头钉住，改完才红
- 新增 `tests/tab-auto-refresh/entry-points.test.mjs`（9 条）：右键菜单与快捷键这两条"弹窗之外的人入口"
  （`contextMenus.onClicked` / `commands.onCommand`）此前零覆盖——菜单点击连派发口都没有。六处对照实跑过。
  一处诚实记录：菜单那道"拿不到标签页就返回"的判断，光断言"落盘状态没变"是钉不住的（判断删掉后
  `startTask` 自己抛、异常被菜单的 `catch` 吞掉，存储一个字节都不差，对照实跑全绿），
  补上数 `tabs.get` 次数的断言才真的变红
- 新增 `tests/tab-auto-refresh/heartbeat.test.mjs`（10 条）+ `tests/tab-auto-refresh/outbound.test.mjs`（11 条）：
  静默心跳与两条外发出口第一次端到端跑通。心跳侧钉的是请求形状（`Range` 必须是 `bytes=数字-数字` 那个
  CORS 安全名单写法）、416 去掉 Range 重试一次、5xx 连续两次才暂停、回到 2xx 连击清零并静默解除、
  落地地址是登录页才算掉线信号、401/403 走掉线不走错误页、离线什么都不留、验证墙的暂停不由心跳解除；
  外发侧钉的是三道闸（地址合法、事件勾选、微信总开关与凭据齐不齐）、载荷三个别名字段、
  `stable_token` + 40001 清缓存重取重试一次、非令牌错误码不重试、失败留痕的 `kind` 与 `errorKey`。
  合计 21 处红→绿对照实跑过（心跳 8、外发 13），每处只红在它点名的那一条（两处例外如实记下：
  阈值改成"一次就暂停"会连带红第二条用例，删掉令牌缓存写入会让两条都红）
- 新增 `tests/tab-auto-refresh/cookie-backup.test.mjs`（13 条）：cookie 备份的**执行器层**第一次在真数据上跑
  （此前桩件的 `cookies.getAll` 恒返回 `[]`，采集、还原、封顶全在空数据上空转）。采集侧钉查询面
  （域链只有 `shop.example.co.nz` 与 `example.co.nz` 两层，`co.nz` 一层都不许问）、同根域内三条都进备份而
  别家的一条进不来、拿不出可信注册域的站点一笔不查不写、开关关着不查、外链漂移不写进别的站点备份、
  确认掉线时"采集确实跑到了但不落盘"、疑似只并计数不覆盖好备份、200 条封顶留票据切杂项；
  还原侧走 `onStartup` 的 `prune`，钉 `hostOnly` 三分支（专属票省略 `domain`、域票照写、v1 缺字段沿用旧行为）、
  已过期条目不写、历史越界备份先收敛再恢复、后缀键整条删掉不留空壳、开关关着时遗留备份清空。
  `alarm-gate.test.mjs` 另加 2 条同站判据（只是同公共后缀算漂移、同注册域的子域跳转算同站）
  - 对照方法学差异：A2 改的三件事里 `siteRoot` / `domainChain` / `planBackupConvergence` 都在
    `shared/logic.js`，而 `logic.test.mjs` 直接 import 真源码，harness 的 `TAR_BG` 只换 `background.js`、
    够不着那条路。所以这批对照复制的是**整仓**（不含 `.git` 与 `_code-review`）到仓库外，
    在副本里分别改坏 `logic.js` 与 `background.js`，再在副本里跑那三个文件
- 桩件侧的一条反向事实，两条新链路都验过：把 `globalThis.fetch = env.fetch` 那一行摘掉，
  心跳文件红 8/10、外发红 7/11，而**绿下来的正好全是"断言一笔都不发"的否定式用例**。
  这就是 A6 第 9 条修之前"外发链路一行没跑"的直接证据，也记成一条一般结论：
  只断言"没发、没写、没通知"的用例，永远证明不了一条链路被执行过
- `env.fire.installed` 同样是死入口，现在被两条旧格式任务的执行器用例用起来：安装/更新那回补记当前网址、
  浏览器重启那回只能淘汰。两处注册实参对调的对照各只红在对应那一条上
- `prune-plan.test.mjs` 的"什么都没要改时不写盘"改成正面数 `tasks` 写入次数，不再比前后 JSON
- `keyword-inpage.test.mjs` 的"注入体不得引用模块作用域"从四个名字的枚举黑名单换成扫标识符：
  原先任何一个新加的模块函数调用（`hostOf(`、`clipOneLine(` …）都能漏过去。现在按 `logic.js` / `config.js`
  的真实导出集比对，新增导出自动进守卫范围
- 文档：`BACKLOG.md` 记下已修与在账的条目（本批之后 A1、A2 与 A6 全部收口，A6 第 8 条的 cookie 执行器用例随 A2 交付），
  `AGENTS.md` 补上焦点三态这条约束、"关掉被监控的标签页会重开一张"的行为、cookie 采集范围的三道约束
  （形状规则 / 域链地板 / 取不到可信注册域就不备份）、新增"测试纪律"小节
  （外发用例必须有真应答桩件；只断言"没发、没写、没通知"的用例永远证明不了链路被执行过），
  以及本机 node 门禁的实际恢复路线（`winget` 那条在非交互环境下装不出任何东西，改用官方便携压缩包）；
  `README.md` 把备份范围从"与监控目标同站的站点"改成"以该站点的注册域为界，公共后缀不会被当成站点"
- 本机验证环境：Node v24.21.0 以官方 win-x64 压缩包装在 `%LOCALAPPDATA%\node-tools\`，不在 PATH 上，
  验证清单第 1、2 条按绝对路径调用；CI 仍是最终裁决
- cookie 备份的读法收成一条判据：**存档清单只能来自一次全量读**（`BACKLOG.md` A13；它推翻的是
  同一天早些时候落地的 A12 第 2 条）。2.1.0 及更早是停任务、启动收敛、备份清理各扫一遍 local，
  最坏每次启动三次全量读——而全量读会把所有键一起反序列化进 SW，包括那几 MB 明文票据，
  换来的只是拿键名做一次前缀过滤
  - 当天先按"主机名清单 `cookieBackupHosts` + 定向读"做过一版，**同一批门禁全绿**，第二轮审计
    才把它打下地：登记是清单自己的一次无锁读-改-写，两个标签页并发备份会盖掉一条，写完存档、
    登记之前 SW 被回收也会漏一条。清单少一条的后果不是慢——恢复与清理此后都按清单取数，
    那条明文存档变成"任何删除路径都看不见"的死数据（实测：关掉备份开关、清单里没有这一家时，
    走完一次 `onStartup` 之后 `cookieBackup:mail.example.org` 原样躺在 local 里，清单已归零）。
    登记在下次页面加载时会自愈，但那正好覆盖在"重启后恢复"这条最要命的路径之前
  - 现在的净结果：一趟启动从三次全量读并成两次（收敛/恢复一次、清理一次），`readBackupEntries`
    是唯一的存档枚举口，`cookieBackupHosts` 键连同 `planBackupFetch` / `planBackupIndex` 一起删除。
    省下的读只能靠"一次读多个调用点共享"来换，不能再靠第二份真相来源
  - 纪律 3：删掉的键从未进过任何已发布版本，没有需要迁移的现场；开关、默认值、TTL、20 站额度、
    权限一个都没动
  - 门禁：`logic.test.mjs` 删 5 条索引纯函数用例，`cookie-backup.test.mjs` 删 6 条索引执行器用例、
    换 5 条"清单说了不算"的用例——先造出清单外的孤儿存档（`cookieBackupHosts: []` 与两条真实存档并排
    种下），再证明它在启动、关闭开关、停任务三条路径上都被删掉；外加一条源码扫描，拦"登记写回"
    这种没有读侧症状的复活（该处改坏实跑只红这一条，所以它不是装饰）。5 处改坏 + 3 处反向绿的
    实跑红名单记在 `cookie-backup.test.mjs` 末尾，上一批索引的对照同时标注为已作废

## [tab-auto-refresh 2.1.0] - 2026-09-18

### Added
- 系统通知可以点开了。点「关键词命中」「任务已停止」「自动暂停」「站点待重登」任一条，会把对应标签页带到前台并聚焦它所在的窗口，通知随之收掉；标签页早就关了就不做事后提示。不需要新权限：`tabs` 已经给到 `windowId`，`chrome.windows` 随 `tabs` 可用

### Changed
- 「`webhookEvents` → `notifyEvents`」的兼容判断合并为 `shared/logic.js` 的 `normalizeStoredSettings`，后台与弹窗共用一份（原先各写一份，后续容易分叉）；且返回值不再携带旧键——设置写回存盘时顺手清掉遗留的 `webhookEvents`，不再随 `storage.sync` 在各设备间空转
- 后台的生效偏好设置改成带内存快照，用户可见行为不变。`getSettings()` 原先每次都并发读 `storage.sync` 与 `storage.local`，而一次任务页加载周期里它被调到 5~7 次（保活同步、验证墙探测、cookie 备份、刷新、心跳、备份收敛各一次）——20 个 30 秒任务就是每分钟 200 笔以上扩展存储读。现在冷启动读一次、sync 有值时不再回读 local，其余走快照。作废点：三个写入侧（local→sync 迁移、`rememberLastInterval`、`save-settings`）各读写两头一次，加 `storage.onChanged` 回流一次
  - `save-settings` 那两处缺一不可：它的合并基座正是 `getSettings()` 的返回值，读到写前的快照会把用户这次没碰的开关按旧值一并写回 sync。这与 2.0.0 修过的「刚填完凭据点发送测试，得到的是配置不完整」是同一类竞态，不能由缓存重新引入
  - 读盘途中发生的作废不回填快照（epoch 比对）。缺这道守卫时，那次已被作废的旧值会在落地时复活成当前值
  - 新增 `tests/tab-auto-refresh/settings-cache.test.mjs`（5 条，chrome 桩件、无需浏览器、进 CI）。按仓库的方法学红线做过红→绿对照：指向改前源码时红在「只读一次」与「local 迁移」两条（后者是 `localGet` 从 1 变 2，正是被省掉的那笔），逐个拆掉三个作废点各红在对应用例上。对照做法记在文件末尾
  - AGENTS.md 的存储小节加了一条硬约束：今后新增写 `chrome.storage.sync` 的地方必须显式作废，否则静默失效
- 关键词匹配改在页面里执行（`executeScript({func: matchInPage, args: [keywords]})`），`shared/logic.js` 的 `pickHits` 拆成 `presentOf`（在场判定）与 `newlyOf`（新出现判定）；拆完的组合入口 `pickHits` 已无生产调用方，一并删掉，原有那条用例改测这两个新函数
  - 注入体必须自包含：`executeScript` 是把函数 `toString()` 后送到页面执行的，引用模块作用域（连 import 进来的函数一样）会在页面里变成 `undefined`，抛出的 `ReferenceError` 又被外层 `catch` 吞掉。所以匹配逻辑是两份实现，由新门禁切出真实源码执行、与 `presentOf` 逐条比对钉住
  - 取文本继续用 `innerText` 而不是看着更省的 `textContent`：后者会把 `<script>`/`<style>` 的源码文本和 `display:none` 的隐藏文字算进正文，等于新增两类用户在页面上看不见的误报
  - 「正文与上次相同就提前结束」的早停删除：回传的已经不是正文，没有可比的字符串，三个采样点固定跑完。这是本次改动的代价，换来的是不再搬正文
- 新增 `tests/tab-auto-refresh/keyword-inpage.test.mjs`（8 条，进 CI）：49 组语料 × 关键词集比对两份实现、自包含约束、`innerText` 约定、深处关键词、空 `body`/空值/非数组入参。四条红→绿对照实跑过（指向改前源码、改成调用模块函数、换成 `textContent`、把截断加回来），结果记在该文件头部注释
- 启动恢复的挂接顺序从 `prune` 那 130 行命令式循环抽成 `shared/logic.js` 的纯函数 `planPrune({ tasks, tabs, adoptLegacyUrls })`，`prune` 只剩"按计划调浏览器"。认领/避让/淘汰的规则因此能被单测直接断言，而不是靠读代码推断顺序
  - 等待窗口改成所有候选共享一轮：原先每个认领不到的任务在 for 循环里各 `await` 一次 20 秒 `watchForUrl`，**且整段时间持着任务锁**——5 个未认领任务就是弹窗起停与到点刷新停摆 100 秒，这是"浏览器重启后弹窗卡住"的直接原因。现在所有候选共用一个上限窗口，且在锁外等。锁内也只剩几次开页与一次写盘（长 await 链被 MV3 回收掐断的概率随之下降）
  - 计划跑两遍而不是"锁外决策、锁内套用"：等待期间用户可能起停过任务、页面也可能正好到位，等完之后要用最新现场重跑一遍计划，认领与重开走同一套规则
  - `_code-review/verify-prune-order.mjs` 与它的生成器退役：它是从 background.js 切出 prune 副本来测的，这一批 prune 整个换写法之后它仍然全绿，测的已经是不存在的代码。同一个场景改由 `tests/tab-auto-refresh/prune-plan.test.mjs`（16 条，进 CI）守，脚本留在本地当静默失效的样本
- `onAlarm` 到点之后的六段提前返回抽成 `shared/logic.js` 的 `decideAlarmAction`（纯函数，输入是已经取好的事实，返回 `{ action, reason }`），后台只负责取齐事实、执行结论。三条次序连同"为什么不能换"的注释一起搬进去，单测可以直接钉住
- 「有活动时跳过刷新」补第二条判据，两条是 or。原来只认内容脚本上报的时间戳（会话态 `rt:activity:<tabId>`），注入失败的页面（受限页、时序、站点权限）永不上报，于是该页在用户眼皮底下被反复重载。现在该页是**所在窗口的活动页、且那个窗口是焦点窗口**也算有活动，焦点窗口 id 跟 `windows.onFocusChanged` 记，认不出来一律当作不在看（宁可多刷一次，绝不能反过来变成永不刷新）。不需要新权限，`tabs` 已经给到 `tab.windowId`
  - 受这条影响的只有 `skipOnActivity` 开着的用户（含从没碰过、按默认开的）：以前"人正看着这页但脚本没上报"会刷新，现在不会。默认值本身一个都没动（60 秒窗口、20 秒等待上限、各开关默认态照旧），`ACTIVITY_SKIP_MS` 只是从后台常量挪到 `shared/logic.js`，弹窗不读它
  - 可见性查询排在 `skipOnActivity` 判断之后才做，开关关着时不会为每次触发去问一遍窗口焦点
- 新增 `tests/tab-auto-refresh/alarm-gate.test.mjs`（20 条，进 CI）：10 条打纯函数表（三条次序、60 秒窗口两侧、or 的两条腿、`NaN`/`Infinity` 一律放行、休眠标签页开关），10 条驱动真 `onAlarm`。五处红→绿对照实跑过，其中"去掉可见性查询的惰性守卫"第一遍是绿的——用例自己先 `focusWindow(1)` 把窗口 id 灌进了缓存，被数的调用一次也没发生，去掉那次调用才红起来。两件事都记在该文件末尾
- 测试桩件（`tests/helpers/background-harness.mjs`）两处修正，都会影响今后所有驱动真后台的门禁：每个 `chrome.*` 事件对象补 `removeListener`（源码里"临时挂、完事摘"的写法此前会在定时器回调里抛 `not a function`，表现为无关用例莫名失败）；事件派发从"取数组末尾一个"改成"只派本次 boot 之后注册的那一段"（后台自己会临时挂监听，末尾那个恰恰是它，旧写法会把模块常驻的处理器整个跳过）

### Fixed
- 浏览器重启或会话恢复后，被重新挂接、重新打开的任务只剩自动刷新，没有静默 HTTP 心跳。`prune` 里 arm 刷新与心跳排在 `setTasks` 之前，而 `ensureHeartbeat(新 id)` 要按落盘后的键读任务，于是它读到"这个 id 没有任务"，反手把刚要建的心跳 alarm 清掉。长间隔任务（小时级）等于整个周期内都不保活，登录会话正好在这段里过期——心跳存在的意义就是救这种会话。现在统一成先写盘再挂表（`startTask` 与 `reopenTaskTab` 本来就是先写后挂，只有恢复这条反着来），`死 ID 的任务重挂到活页面` 用例把 `hb-20` 必须建出来钉住
  - 这一条是真重启验过的：把任务键改成不存在的假 id 后关掉浏览器进程、用同一 profile 带 `--restore-last-session` 重开，任务按网址认回真实标签页，`refresh-` 与 `hb-` 两条 alarm 都在，也没有多开标签页（`_code-review/verify-22-reclaim.mjs`，本地门禁，同时验到"正被看着的页不重载 / 后台页照刷"两条与关键词页内匹配在真页面上检到中文）
- README 的「全局暂停」补明确语义：暂停只停自动刷新，静默心跳与后台保活继续维持登录会话。暂停不停心跳是有意设计——停了反而可能让登录过期，此前文档容易读成"全部后台活动都停"
- 排在正文 300KB 之后的关键词永远检不到。检测链原先把整页 `innerText` 截到 300000 字符再序列化回 service worker 做匹配，长列表页、长文页面的目标内容一旦落在截断点之后就属于静默漏检——页面确实被扫了、也按时通知了"未命中"，看不出任何异常。现在匹配搬到页面里做，只回传命中的关键词名，截断随之删除。真浏览器实测：一页 486,040 字符的正文，排在约 430KB 处的关键词（中英文各一条）都能命中，同一份语料按旧的 300KB 截断则一条都检不到
- 系统通知只创建、从不清理。四类通知一旦发出就长期留在通知中心：任务早就停了、站点早就重新登录了，通知还在，等于让人对着上个周期的结论做判断。README 的功能表和 AGENTS 的手工清单里都写着"自动清理通知"，代码里其实没有这条路径。现在补 `notifications.clear` 的时机，且按时机分而不按"任务还存不存在"一刀切：重新开始某标签页的任务清掉它上一轮的三条；停任务只清"自动暂停"那条——"关键词命中"往往正是命中即停任务的产物，跟着 `stopTask` 一起清等于把用户刚收到的通知当场撤回；从自动暂停恢复时才清暂停那条；站点重新登录成功、或该站点不再被任何任务监控时清"待重登"那条
- 一次掉线可能发出两条通知。`session-lost` 的 ID 原先用整页主机名，而 SSO 常落在兄弟子域，于是 `www.example.com` 与 `sso.example.com` 各发一条几乎一样的。ID 现在归一到注册域，与掉线探针用的键一致，通知的创建、清理与点击反查才不会各说各话
- 新增 `tests/tab-auto-refresh/notifications.test.mjs`（13 条，进 CI）：各清理时机（含"不该清的时候别清"两条反例）、注册域归一、点开跳转、死标签页与陌生 ID 不抛错，另有一条扫源码的守卫确认 ID 只有 `NOTIF_ID` 一个出口。四条红→绿对照实跑过：删掉 startTask 的清理、让 stopTask 连命中通知一起清、删掉探针恢复的清理、把 ID 归一去掉，各只红在对应那一条上
- 桩件收拢成 `tests/helpers/background-harness.mjs` 共用。`background.js` 在模块顶层注册监听器，每新增一个 `chrome.*.addListener` 就会让每一份手写桩件在 import 那一步抛错——这次加 `notifications.onClicked` 时就当场把 5 条无关用例打红了。`_code-review/` 里两个自带桩件的门禁同步补上该接口

## [tab-auto-refresh 2.0.0] - 2026-09-15

> 1.7.0 与 1.8.0 曾作为开发版本号存在，均未单独打 tag 发布；本 2.0.0 是首次包含两者全部内容的发布版本。

### Added
- 微信直连通知（默认关闭）：扩展的 service worker 直接调用腾讯官方接口（`api.weixin.qq.com`）把通知推成微信模板消息，不经任何中继服务器、也不用第三方推送服务商，手机上不需要装任何 App。填入四项凭据（appID、appsecret、openid、模板 ID，来自微信"公众平台接口测试账号"，扫码即申请、免资质、免费）即可，与 Webhook 共用"通知哪些事件"的勾选，两者可以同时开启
  - 可行性是实测出来的：同一浏览器、同一目标地址，网页语境 `fetch` 被 CORS 拦（`Failed to fetch`），扩展 service worker 语境返回 HTTP 200 和微信业务错误码（`40013 invalid appid`，说明请求确实抵达并被微信处理）。`manifest.json` 已有的 `<all_urls>` 主机权限覆盖了它，没有新增任何权限
  - 令牌走官方 `stable_token`，不用老的 `/cgi-bin/token`（后者每刷一次就作废上一个，多端并发或多次重启会互相打掉）。令牌缓存在 `chrome.storage.session`（SW 闲置 30 秒就被回收，放内存等于没有），距过期 5 分钟内提前重取，命中 40001/42001 时清缓存重取并重试一次
  - 失败不留白：微信错误码翻成"该去哪改"的提示（40013 是 appID 与密钥填反，43004 是还没关注测试号，40164 需要 IP 白名单，40037/47003 是模板不匹配，45009 是额度用尽），最近一次推送结果写在 `chrome.storage.local`，弹窗实时显示
  - 弹窗新增"微信直连"开关，并入既有的双列网格（第 10 格，不新增网格行）；配置走二级视图整页切换，四项凭据与模板正文的填写提示都在那里，避免四行输入框把弹窗顶破 600px 上限
- 微信直连配置教程页（`wechat-setup.html`，中英双语走语言包，页面本身不发任何网络请求）：从弹窗的微信配置页顶部"配置教程"直达，讲清四项凭据去哪拿、模板为什么必须一字不差、错误码怎么对号入座、凭据存在哪。教程文件就在仓库里，不依赖外部文档站。步骤一、步骤二各配一张测试号页面的实拍图（`wechat-guide-1.png`、`wechat-guide-2.png`），标出 appID、appsecret、openid、模板 ID 四处位置，图里的凭据已整段替换为示例值，不含任何真实数据

### Changed
- 通知事件清单的键名从 `webhookEvents` 改为 `notifyEvents`（webhook 与微信直连共用一份定义）。旧键里已有的勾选会自动接续，不会被新默认值覆盖成全选
- 弹窗的间隔输入改成"预设下拉与自定义秒数"互斥：下拉新增"自定义…"项，选预设就清空秒数框，秒数框一有数字下拉就切到"自定义"，秒数框被清空则回落默认预设。任何时刻只有一个入口持有值
- 微信卡片标题的事件名改用一套短名（新增 `wechatEvKeywordShort`、`wechatEvStoppedShort`、`wechatEvPausedShort`、`wechatEvSessionShort`，中英文各一份）。弹窗复选框上那套标签太长（英文最长 17 个字符），会把 20 字预算吃光、站点名放不下，两者用途不同不能再共用
- `wechatTitleSep` 改为自带空格（中文 `" · "`、英文 `"·"`）。分隔符占几个字符是排版决定，写死在代码里就没法按语言调预算；`wechatTitleOf` 不再对它做 trim，中文的渲染结果不变

### Fixed
- 英文界面的微信卡片标题不再切出像另一个域名的碎片。20 字预算减去事件名后只剩 0~6 个字符，而 `clipHostTail` 的 label 边界分支要放得下 `example.com`（11 个字符）才会命中，于是英文的每一条卡片都落进硬截分支：实际发出的是 `keyword hit - …e.com`、`session lost - …com`，`task auto-stopped` 与 `task auto-paused` 更直接把站点名整段挤掉。三处一起改：`clipHostTail` 放不下完整注册域时返回 `null`，由 `wechatTitleOf` 决定整段丢掉站点，绝不给碎片（无点主机仍从左侧截）；卡片标题改用上面那套短事件名；分隔符自带空格，把省下的字符留给站点。新增门禁 `verify-wechat-title-i18n.mjs`（两语言 × 五事件 × 六站点穷举真实标题），修复前 60 个标题里有 6 个碎片、8 个只剩顶级域、12 个无站点，修复后是 0、0、4（剩下 4 例都是没有站点的"测试消息"）
- "发送测试消息"前补上 `await saveSettings()`。凭据输入框是失焦保存的，点这个按钮必然先让输入框失焦，所以弹窗会先发 `save-settings`；两条消息在后台各自独立执行，而 `save-settings` 自己也要先 `await` 一次 `getSettings()` 才能写盘，于是 `wechat-test` 读到旧值：刚把四项凭据填全、点测试，得到的是"还缺 appID、密钥、openid、模板ID"，而实际发出去的微信请求数是 0。这是首次配置的主路径。新增门禁 `verify-wechat-save-test-race.mjs`，断言 `wechat-test` 的发出晚于 `save-settings` 的回调落地
- README 的"尊重你的操作"默认值写反了：功能列表写的是"默认关闭"，而 `DEFAULT_SETTINGS.skipOnActivity` 从 `4fad900` 起就是 `true`，CHANGELOG 与 AGENTS.md 写的都是"默认开启"。这条不只是文案，它同时是内容脚本注入的开关之一，说反了等于让用户以为默认不会往被监控页注入活动监听
- `clipOneLine` 改按码点截。原先按 UTF-16 码元 `slice`，被监控关键词里含 emoji 且恰好压在截断点时，会切出一个孤立代理，卡片上显示成乱码方块。平台数的是"字"，码点数才是对的账
- 微信卡片不再被平台砍成半句话：字段改为按平台的 20 字硬上限发送。依据是 2023-05-04 生效的《关于规范公众号模板消息的再次公告》：中间主内容的单个字段不超过 20 个字、不支持换行，超长由平台直接去掉且不给任何提示，首行与尾部字段被整体去除。此前上限按 400 字设，实际发出去的是 49~81 字的多行正文，平台砍到第 20 个字，用户只看到半句话，而且站点名那一行必然丢（平台先去掉换行把多行连成一串再砍，20 字全被第一行吃光）。现在标题是"事件 + 站点"，正文是按事件给的一句话结论（中文最长 12 字），页面地址不占字数、走卡片点击跳转。自己截还有个好处：省略号由我们给，用户能分清"就这些"和"被切了"。新增门禁 `_dump_wechat_payload.mjs` 第七节逐字段判长度与换行
- 全局显式声明 `[hidden] { display: none !important }`。`hidden` 属性走的是浏览器默认样式，作者样式里的任何 `display`（比如 `.row{display:flex}`）都会在同特异性下压过它，于是新增的微信那行开关关着也照样占高度，而"总高不超过 600px"这条门禁看不出来。高度门禁另外补了"开、关两态该行高度必须不同"的断言
- 事件白名单不再把空数组当假值。原写法是 `Array.isArray(x) && x.length ? x : 默认集合`，于是"四项全部取消勾选"仍会照推；现在统一走 `notifyEventsOf(settings)`，空集合即完全不发
- 弹窗间隔输入的静默不一致：预设下拉与自定义秒数原先都没有 `change`/`input` 监听，在秒数框填过 90 之后再选"每 5 分钟"，界面显示 5 分钟、实际按 90 秒跑，而且只在 4 秒后就消失的提示行里能看见真值。现在两个入口互斥，并新增门禁 `verify-interval-pick.mjs`，直接捕获"开始"发出的秒数
- `backupCookies` 里"会话掉线"的通知补上 `await`。同一函数在行为通道里本来就是 `await`，只有备份这条漏了，而 `notifySessionLost` 里含 webhook 与微信的 `fetch`，裸甩会在 SW 回收时被截断，丢的正是人不在电脑前最需要的那条
- cookie 备份的 200 条截断改为"票据优先"排序。原实现按 `chrome.cookies.getAll` 的返回顺序切尾，而这个顺序未定义：会话票据落在尾部时每次备份都稳定缺它，掉线检测也因为前后样本都缺票据而恒判正常，表现为备份时间戳一直在更新、重启后却恢复不出登录态。新的 `capCookies` 只在超限时按 httpOnly、会话票、`__Host-`/`__Secure-`、`path=/`、域更短的顺序排序切尾，正常规模保持原始顺序
- `sessionProbe` 增加收敛。站点掉线后探针 `lost=true`，用户停掉该站任务时探针没有任何清理路径，于是稍后在同一站点重建任务时，`startTask` 里那次"首次刷新前关掉浏览器"的备份会被 `isProbeLost` 跳过。现在 `pruneCookieBackups` 按"仍被任务引用的根域"收敛探针，顺带止住条目的无界增长
- 关掉"重启后恢复登录"时立即清掉遗留备份。README 承诺"关闭状态下不备份、不恢复，遗留备份也会被自动清除"，而清理原先只发生在 `stopTask` 与启动 `prune` 里，用户读完安全说明关掉开关后，含 HttpOnly 登录票据的明文 cookie 仍躺在 `chrome.storage.local`。现在 `storage.onChanged` 把 `cookieBackup` 一并纳入收敛条件
- CHANGELOG 1.7.0 的 Added 条目恢复到收窄后的口径：同版本 Added 写着"标题/正文关键词"，而 Fixed 写着"不再扫 `document.body.innerText`"，自相矛盾。另把"测试号…无时间限制"这一无法核实的说法改为"免资质、免费"

- 微信直连教程页与弹窗里的模板内容示范，原先写成裸变量一行（`{{title.DATA}} {{content.DATA}}`），不符合微信平台对模板内容的解析规则——变量前必须是「关键词名称：关键词内容参数」（中文冒号）的组合，裸变量整行会被平台丢弃、且接口照常返回成功，照抄示范的用户会得到只有模板标题的空白卡片且没有任何错误码可查。已改为「标题：{{title.DATA}}」/「内容：{{content.DATA}}」两行，弹窗常驻提示与悬停提示同步修正，并新增门禁 `verify-wechat-template-doc.mjs` 盯住"给用户看的模板示范必须合规"
- README 的弹窗截图更新为当前版本实拍：原图摄于微信直连上线前（9 个开关、无「微信直连」一格），与实际界面不符；现以真实加载的扩展实拍替换，并新增微信开启态的第二张截图（`docs/tab-auto-refresh/popup-wechat.png`）

## [tab-auto-refresh 1.7.0] - 2026-09-11

### Added
- 「后台保活」功能（默认开启，可在弹窗关闭）：向监控页面按标签页级注入保活脚本，派发模拟鼠标/键盘活动（首个心跳 12~20 秒、之后 45~75 秒随机），延缓“按用户交互计时”的服务器端会话过期；任务停止、重开、浏览器重启与开关切换时自动收敛
- 会话失效检测（双通道）：状态通道：备份 cookie 时发现会话票据从有到无，连续 2 次采样确认（防站点换票节奏误报；疑似期间绝不用坏样本覆盖最后一次好备份）即冻结该站点备份并通知（按站点 6 小时节流）；行为通道：任务页落在登录页 URL、或静默心跳收到重定向到登录页 / 401 / 403，同样计入确认，掉线期间角标变红提示
- 「静默心跳」（默认开启）：每 4 分钟向监控地址发一次带 cookie 的后台 GET，不打扰页面地续住“按最后请求计时”的服务器端会话
- 关键词监控：创建任务可选填关键词，页面加载完成后检测正文，命中即系统通知并自动停止任务（从“定时刷新”到“页面监控”）
- 调度抗抖动：刷新触发时间在原周期上 ±15% 随机（下限 30 秒），多任务不再同拍齐刷；系统从睡眠唤醒后自动把所有过期定时器打散重建，消除睡眠漂移
- 重启恢复增强：认领不到目标页的任务先经 20 秒延迟认领窗口（等登录跳转 / 会话恢复晚到的页面到位），等不到再重开；修复两个任务争抢同一标签页时后者被静默丢弃的边界
- manifest 新增 `scripting` 与 `idle` 权限；站点权限保持常驻 `<all_urls>`（曾尝试改为按站点动态申请，实测系统授权框会夺走焦点关闭扩展弹窗、导致必须二次点击「开始」，对自用分发场景得不偿失，已回退并记录决策）
- 弹窗任务行对 `skipDiscarded` 命中的休眠标签页显示"休眠中，已跳过刷新"，消除唯一的静默跳过（可解释性）
- 关键词监控增强：多关键词（逗号分隔，上限 10 个）；命中检测改为"加载后立即 + 3s + 10s 有界重采样"，SPA/迟渲染页面不再漏检，正文稳定即提前结束；"命中后继续监控"可选：默认命中停任务的行为不变，开启后持续盯守、按在场集去重仅新命中才通知
- Webhook 通知（默认关闭）：会话掉线 / 关键词命中 / 任务自动停止 / 任务自动暂停四个事件按勾选 POST JSON；载荷同时填充 content/text/body 三个字段别名，Discord / Slack / Telegram / Ntfy 与自定义端点开箱即用；README 安全说明补披露面警示
- 异常自动暂停：静默心跳连续两次 5xx/404，或页面命中验证墙特征（文档标题 + Cloudflare/reCAPTCHA/hCaptcha 挑战域名资源，不扫正文），任务级 `autoPaused` 暂停刷新（定时器保留、恢复零重建），通知 + Webhook + 角标 `⚠` 橙第五态；错误页恢复 2xx 自动解除，验证墙由用户点"恢复"按钮解除，与掉线状态机完全隔离，不污染备份冻结语义
- 尊重用户操作（默认开启）：注入脚本按 `event.isTrusted` 识别真人操作并节流上报，后台在 60 秒内跳过该页刷新；只"跳过"不"重置计时"，防止用户操作把刷新无限推迟。代价是它同时是内容脚本注入门控之一，默认开启即默认向被监控页注入活动监听（合成事件 `isTrusted` 恒为 false，不会被误判为真人）
- 防系统休眠（默认关闭）：`chrome.power.requestKeepAwake("system")`，有任务且开关开启期间持锁，任务清空即释放；README"机器不休眠"前提转由扩展职责承担
- manifest 新增 `power` 权限；注入通道按职责解耦（心跳/活动监听各有开关，任一开启即注入、配置热推送），关闭保活不再影响其他注入功能
- 「验证墙保护」开关（默认开启，弹窗可关）：验证墙探测此前无法关闭，而它一旦误判，任务会卡在暂停态直到手动恢复，现交由用户显式控制

### Changed
- 弹窗文案精简：8 个开关的可见标签从「名称 + 整句解释」压成短标签（例：「后台保活：定期向监控页面注入模拟活动，延缓服务器端登录过期」→「后台保活」，「你在操作该页面时跳过刷新（60 秒内有真人操作即跳…）」→「操作时跳过刷新」），完整解释改由悬停提示承载（新增 `data-i18n-title` 渲染通道与 8 条 `*Hint` 键，中英文同步）。界面可见文案字数约减半，设置卡片不再折行堆叠，信息不丢失
- 弹窗尺寸重做，消除滚动条：Chrome 弹窗外框高度上限是 600px，而此前即使没有任何任务弹窗也已 610px，任务一多更是直奔 876px，滚动条必然出现。现在宽度 360→400px，9 个开关改为双列网格（9 行压成 5 行），间距整体收紧，任务列表封顶 2 行（第 3 行露头作为"下面还有"的提示，超出部分在卡片内滚动），并给 body 加 `max-height: 600px` 兜底：内容不足时高度仍由内容决定，超出时由任务列表这一唯一弹性块让步。实测最坏情形：中文 5 个任务 541px、英文 564px、英文 + 三个提示行同时出现 600px，均无滚动条
- 修正 Webhook 兼容性表述：载荷的 `body` 字段并非 ntfy 认的字段，实测 ntfy 只在根端点解析 JSON，POST 到 `/<主题>` 会把整个 JSON 当正文存下，所以它收到的是原始 JSON 文本（且载荷无 `topic` 字段，改填根端点会 400）。README、弹窗提示与代码注释不再宣称「Ntfy 开箱即用」

### Fixed
- 掉线状态通道的决策与落盘映射合并为同一纯函数 `applyBackupAction`，后台写盘、单元测试与回归脚本三方共用，消除"测试复刻映射与后台各写一半"的契约漂移隐患
- 静默心跳去同拍：建 alarm 带随机初始相位、睡眠唤醒重建打散 0~60 秒；请求加 `Range: bytes=0-1023` 截断，不再拉取整页响应体
- 启动恢复中旧 alarm 的清理推迟到任务落盘之后：service worker 中途回收最坏只留会自清理的孤儿 alarm，不再产生"任务在、永不刷新"的静默僵尸
- 掉线行为信号值未变化时不再写盘与刷角标（30 秒任务从每周期一轮无意义写降为零），已确认掉线后疑似计数封顶
- 30 秒档刷新抖动改为只正向：此前约一半样本被 30 秒地板抬回原值，去相关只实现一半
- 修复上一提交引入的回归：启动恢复延后清理旧 alarm 时未排除"本轮刚重新挂接/重开占用的标签页 id"，tabId 互换等场景会把新 arm 好的 alarm 清成"任务在、永不刷新"的僵尸；现按落盘任务表的键集合做互斥守卫（`_code-review/verify-prune-order.mjs` 为该场景常驻回归）
- 会话恢复时清零通知节流时间戳：掉线→恢复→再掉线视为独立新故障，6 小时内同样通知
- 静默心跳收到 416（站点拒收 Range）时自动去 Range 重试一次，避免不规范站点的心跳通道静默失效；首拍相位加 1 秒下限
- 修复本批新增功能的一处共性缺陷：异常连击计数、真人活动时间戳、防休眠持锁标记原先放在 service worker 的内存变量里，而 MV3 的 SW"闲置 30 秒即终止"，两次采样相隔分钟级（心跳 4 分钟、验证墙随刷新周期），计数每次从 0 起，导致"连续 2 次才暂停"永不触发、60 秒跳过刷新与关开关后的休眠锁释放全部失效；现按 Chrome 建议改为 `chrome.storage.session`（跨 SW 回收存活、随浏览器会话结束清空，不落磁盘、无需新权限），读写走独立串行队列防丢计数。常驻回归：`_code-review/verify-sw-restart-state.mjs`
- Webhook 的四处调用点由 `void postWebhook(...)` 改为 `await`：载荷请求必须挂在被 await 的链路里，否则 SW 回收会截断出站请求（与该函数自身注释原本就自相矛盾）
- 被异常自动暂停的任务，其标签页被关闭后同样按老行为停止任务并通知。此前自动暂停的早退判定排在标签页存在性检查之前，会让这类任务无人认领地留在列表里
- 验证墙探测的匹配面收窄到文档标题与挑战域名（Cloudflare / reCAPTCHA / hCaptcha）资源，不再扫 `document.body.innerText`；确认阈值同时由 2 提到 3。原实现把正文前 4000 字一并纳入，正文里出现「验证码 / access denied」这类日常词（登录框提示、帮助文案、页脚）就会把正常页面判成验证墙，而误暂停后刷新循环随之停下、页面不再加载、探测也不再运行 → 任务一直卡在暂停态。其中 `access denied` 一并移除：401/403 的登录墙语义本就另走掉线通道，不该重复判定
- Webhook 地址非法时弹窗给出内联提示：后台对非法地址是静默忽略的（归一化失败即直接返回、连连接都不建），此前界面上毫无反馈，用户会以为已配置生效
- 本地回归门禁 `_code-review/verify-prune-order.mjs` 补上可判退出码：此前无论 ✅/❌ 都是 exit 0，prune 顺序回归复发时脚本照样绿灯

## [tab-auto-refresh 1.6.0] - 2026-09-10

### Added
- 「重启后恢复登录」（cookie 备份）改为默认关闭的开关，开启入口在弹窗；README 新增安全说明
- 浏览器内部页面（chrome:// 等）直接拒绝创建定时刷新任务，弹窗提示同步更新
- 发布工作流新增旧 Release 与 tag 清理步骤，发布面只保留最新版本

### Changed
- 启动恢复按注册域匹配并恢复所有备份主机，兄弟子域（如 SSO 登录子域）的 cookie 备份不再被遗漏；恢复成功后按根域决定是否补刷新
- `domainChain` / `urlKey` / `tabShowsUrl` / `RESTRICTED_URL` 下沉 `shared/logic.js`，并补充单元测试；移除无用的 `moz-extension` 匹配
- 弹窗 `storage.onChanged` 仅在任务 / 暂停 / 设置变化时重绘，cookie 备份写入不再触发全量刷新
- 偏好默认值统一到 `shared/config.js` 的 `DEFAULT_SETTINGS`，后台与弹窗共用
- 弹窗界面 `lang` 属性跟随浏览器语言

### Fixed
- cookie 备份写入失败改为仅告警一次，避免每次刷新重复刷日志

### 注意
- cookie 备份开关默认关闭：升级后原有备份会在下次清理时移除，需要登录保持的用户请在弹窗中手动开启

## [tab-auto-refresh 1.5.0] - 2026-09-10

### Added
- cookie 备份自动清理：站点不再被任务使用、超过 30 天或超过 20 站上限时淘汰，停止任务与浏览器启动时统一执行
- 仓库校验新增中英文语言包键集合齐平检查，防止新增文案漏翻译
- 发布工作流增加 tag 版本与 manifest 版本一致性检查，并在工作流内先跑仓库校验与单元测试，形成发布硬门禁
- 启动恢复挂接改为"标签页认领"：同一网址开在多个标签页时，每个页面至多被一个任务挂接，认领不到页面的任务转入重新打开
- cookie 备份记录 `hostOnly` 属性并标记备份格式版本（schemaVersion 2）

### Changed
- cookie 备份只写入与监控目标同根域的站点，外链漂移不再备份无关站点登录态；单站点备份封顶 200 条
- 启动/挂接标签页事件先经内存任务快照过滤，非监控标签页不再触发存储读写，降低后台唤醒
- 无法获取标签页时启动任务明确报错（新增中英文提示），不再创建无网址的无效任务
- 浏览器启动恢复时，仍存活的 tabId 需网址与任务匹配（精确或 origin+pathname 相等）才保留，防止 ID 复用导致任务错挂到无关标签页

### Fixed
- 修复备份清理误用 storage 通配符读取（该 API 不支持），导致站点淘汰 / TTL / 数量上限三类清理全部失效
- 修复 host-only cookie 还原时被强制写成域 cookie：作用域扩大到子域、`__Host-` 前缀票据写入失败；还原时按 `hostOnly` 区分（缺该字段的旧备份维持原行为）
- 修复 v1.4.3 前无网址的旧任务在浏览器重启后被误认领到无关标签页：扩展更新时补记当前页面网址，浏览器重启时淘汰（旧任务 ID 已无从辨认目标）
- 修复系统通知被关闭时任务清理流程产生未处理异常
- 修复右键菜单 / 快捷键启动失败时的未处理异常
- 修复弹窗源码中三处编码损坏的中文注释

## [tab-auto-refresh 1.4.5] - 2026-09-09

### Changed
- 注释、单元测试与文档中的示例统一改用通用 example 域名，移除特定站点与行业字眼

## [tab-auto-refresh 1.4.4] - 2026-09-09

### Added
- 误开其他网页自动带回监控目标：标签页漂移到完全不同的站点时，下一次刷新导航回原网页，不再改刷误开页面
- 同站跟随、跨站锁定：登录 / SSO 在同站子域间跳转时监控目标正常跟随，只有真正跨站漂移才拉回

### Fixed
- 任务记录的网址仅在目标同站内更新，误开的其他网站不会覆盖监控对象

### Tests
- 新增站点根域 / 同站 / 漂移判定单元测试（11 项全部通过）

## [tab-auto-refresh 1.4.3] - 2026-09-09

### Fixed
- 修复关闭浏览器后任务全部丢失：窗口整体关闭（含退出浏览器）不再触发自动重开与误清理，启动时把任务重新挂接到恢复的标签页，找不回则自动重新打开
- 弹窗打开提速：清理失效任务改为后台异步执行，不再等待后台冷启动，首屏即时渲染

## [tab-auto-refresh 1.4.2] - 2026-09-09

### Fixed
- Cookie 备份按站点独立存储，多个任务并发备份不再互相覆盖
- 备份覆盖父域 cookie（注册域级 SSO 登录票据），修复部分站点重启后无法恢复登录
- 恢复 cookie 后立即重载受监控的标签页，登录态即刻生效，不再等到下个刷新周期
- 自动重开使用的网址会在页面加载后更新，避免重新打开时停留在登录页

## [tab-auto-refresh 1.4.1] - 2026-09-09

### Fixed
- 修复弹窗任务列表重复渲染：开始任务后同一任务可能出现两行相同条目
- Cookie 备份时机修正：开启任务时立即备份一次，避免首次刷新前关闭浏览器导致无备份可恢复

## [tab-auto-refresh 1.4.0] - 2026-09-09

### Added
- 误关闭标签页自动重开：定时刷新中的标签页被关闭时，自动在后台重新打开并继续刷新
- Cookie 备份与恢复：每次刷新前备份域名 cookie，浏览器重启后自动恢复登录状态
- 新增 `cookies` 权限和 `<all_urls>` 主机权限，用于 cookie 备份恢复功能

### Fixed
- 修复关闭浏览器后残留幽灵任务的 bug：prune() 函数添加延迟等待标签页恢复

## [tab-auto-refresh 1.3.0] - 2026-09-07

### Added
- tab-auto-refresh 快捷键启动任务时使用最近一次成功设置的间隔（默认仍为 5 分钟）

### Changed
- Release zip 增加顶层 `tab-auto-refresh/` 目录，解压后可直接选择文件夹加载

### Fixed
- tab-auto-refresh 快捷键描述改为中英文语言包文案

## [tab-auto-refresh 1.2.0] - 2026-09-07

### Added
- 全局暂停 / 恢复：弹窗一键暂停全部任务，角标显示暂停状态；暂停期间不触发刷新，恢复后按原周期继续
- 页面右键菜单：在网页任意位置右键即可设置定时刷新（此前仅支持标签页右键）
- 键盘快捷键 `Alt+Shift+R`：开关当前标签页的定时刷新
- 「跳过已丢弃标签页」选项：开启后休眠标签页不会被自动刷新唤醒
- 英文界面（`_locales/en`），中英文完整支持
- 仓库级校验脚本与单元测试（`scripts/validate.mjs`、`tests/`），CI 质量门禁
- MIT LICENSE

### Changed
- 偏好设置（忽略缓存、跳过已丢弃）迁移到 `chrome.storage.sync` 多设备同步；旧本地设置自动迁移，任务等状态仍存 `chrome.storage.local`
- 手动开始新任务会自动解除全局暂停
- 提取纯逻辑到 `shared/logic.js`（间隔兜底、间隔与倒计时格式化），后台与弹窗共用

### Fixed
- 工具栏图标 `default_title` 改用国际化名称 `__MSG_extName__`

## [tab-auto-refresh 1.1.0] - 2026-09-07

### Added
- 任务写锁（`withTaskLock`），防止弹窗 / 右键菜单 / 定时器并发读写任务列表
- 弹窗每秒同步 alarm 列表，倒计时归零后继续滚动
- 任务失效时自动取消并通过 `chrome.notifications` 通知
- 自定义间隔小于 30 秒时给出明确提示
- favicon 为空时隐藏占位图标
- 国际化骨架（`chrome.i18n`，中文文案迁入 `_locales/zh_CN`）
- tag 驱动的自动发布工作流与项目记忆文档 `AGENTS.md`

### Changed
- `shared/config.js` 统一管理前缀与预设间隔，后台和弹窗共用
- service worker 改为 ES module
- `minimum_chrome_version` 设为 120

## [tab-auto-refresh 1.0.0] - 2026-09-07

### Added
- 首个版本：预设与自定义间隔、忽略缓存刷新、任务列表与倒计时、右键菜单、立即刷新、持久化与自动清理、角标计数
