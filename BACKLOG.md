# BACKLOG.md

待办清单，不是项目记忆。项目约定与"为什么这样设计"看 `AGENTS.md`，已经发生过的事看 `CHANGELOG.md`。
修完一条就整段移进 `CHANGELOG.md` 对应版本，本文件只留还在账上的：A1~A43 与 E1~E3 的结案叙述都在
`CHANGELOG.md`，这里只剩一张索引表（各测试文件末尾按 `BACKLOG.md` Axx 引用编号，编号留着）。

清单来源：2026-09-19 对 2.1.0 代码的一次从零审计，不采信文档结论，逐条对着源码确认。
新增条目要带源码位置和"为什么当时测不出来"，不接受"某处可能有问题"这种形状。位置一律写符号名
（函数名、消息类型、存储键），不写行号：行号每动一轮就漂一次，而且漂的时候没有任何症状。
`tests/tab-auto-refresh/doc-anchors.test.mjs` 扫本文件全文与 `CHANGELOG.md` 的 `[未发布]` 段，
两种行号锚形状都会红。红→绿对照的做法记在每个测试文件末尾，没有对照就等于这条还没修。

## 还在账上的

| 编号 | 一句话 | 状态 |
| --- | --- | --- |
| V1 | 真机手工验证，下面那一节列了每一条 | 待做 |
| 发版 | `[未发布]` 从 2.1.0 之后攒了 A1~A43 加本轮文案对齐，切不切一版出去要人点头。打 tag 与推 tag 都不自动做 | 待定 |

## 只能真机验（V1）

在 `chrome://extensions` 加载插件目录，按 `AGENTS.md` 验证清单第 4 条走一遍。本批新增 (c)~(k)，
(a)(b) 是清单里已有的两条。这一节每一条都是切片用例给不了的现场，不是"还没想到怎么写用例"。
2026-09-20 起 (d)(h)(j) 各减掉一半：那几面本地能量了（`scripts/screenshot-popup.mjs --measure`
真 Chrome 真渲染，读回 DOM 的真实取值判），下面写的是剩下的真机那一半。

- (c) 焦点三态（A1）。开着"尊重你的操作"，把某任务页设为所在窗口的活动页，再把焦点切到别的应用，
  等两三个刷新周期，它必须照常刷新。
- (d) 弹窗回填（A5）。回填的那一半已经本地验了：`--measure` 每一趟渲染都读回关键词框、
  "命中后继续盯守"与实际间隔的取值，跟场景数据对不上就 exit 1（对照 P1~P3 在
  `screenshot-mock.test.mjs` 末尾）。显隐判据与调用顺序仍由 `popup-repopulate.test.mjs` 钉住。
  真机只剩一步：直接点"开始"（先停后起）不能把关键词弄丢——那是写盘路径，截图通道不点按钮。
- (e) 关窗时的写盘（A11）。在 webhook 框里打完字，不按 Tab、不点别处，直接点弹窗外关闭，重开弹窗内容要
  还在。`input` 去抖 500ms 已经写了一笔，关窗时 `pagehide` 与 `visibilitychange → hidden` 各补一次，
  而那两笔发生在文档正在销毁的路上，`sendMessage` 不保证赶得上。所以要验的是残留窗口：打完立刻点外
  （不到 0.5 秒）重开在不在，停一下再点外在不在。顺带两个五秒的检查：非法地址一边打一边出红字；
  凭据填缺时点微信"发送测试"，报错之后按钮要能退回"发送测试"，而不是停在"发送中"。
- (f) 多框架检测（A3）。找一个整页挑战嵌在 iframe 里的站点（顶层只剩空壳标题那种），开任务后等三个
  加载周期，要被自动暂停、角标变 `⚠`。反过来，页面上只有一个验证挂件（reCAPTCHA / hCaptcha 那种小框）
  的正常页面不该被暂停。这里要的框架尺寸与自身网址得是真实 iframe 才作数。
- (g) 哈希路由的落点（A4）。给一个前端哈希路由的页面（形如 `https://x.test/#/orders/42?tab=1`）开任务并
  收到外发通知，点开微信卡片或 webhook 里的链接：A4 把 query 与 hash 一起剪掉了，落点会是
  `https://x.test/` 即应用根，而不是 `#/orders/42` 那一页。剪的收益（一次性令牌不出机器）由
  `outbound.test.mjs` 钉住了，"落点还是不是用户要找的那一页"只有真机说得清。要验的是这件事对日常使用
  碍不碍事：不碍事就维持现状，碍事再谈怎么在不回吐 query 的前提下带上深链。
- (h) 多出一行之后的高度（A8、A28）。在 webhook 框里填合法地址，弹窗会多出"发送测试 + 状态"那一行。
  "会不会把页面顶破"与"更长的中文站点名会不会把某一行挤出换行"两样都就地量掉了：
  `node scripts/screenshot-popup.mjs --measure` 把弹窗按七个形状各真渲染一遍，最深底边全在 600px 内
  （最紧的一种仍是"填了合法 webhook 地址"），长中文站点名那一格另外要求两条标题真被截断（对照 P6、P8）。
  剩下两个变量量不到：系统字体回退（会改每个字的宽度）、浏览器 zoom（CSS 像素与 zoom 无关，
  理论上不新增换行，但没有本机之外的样本）。
- (i) 中文冒号是必需还是仅仅充分（A10）。仓库里从没实测过，依据只有 2.0.0 当时的调查记录加官方文档示例
  的写法。A10 因此不让教程为"换成英文冒号再试"这种未证的兜底留话。真要结案得有微信测试号：按
  「标题：{{title.DATA}}」「内容：{{content.DATA}}」两行建模板，只把中文冒号换成英文 `:` 再发一次，
  看卡片是空白还是照旧渲染。英文冒号也认的话，`guideStep2Why` 那句"微信只认「关键词：{{变量}}」"就说
  过了头，要改成"要有关键词加冒号，中英两种都收"，同时放宽 `wechat-copy.test.mjs` 的"只教一种冒号"；
  不认则现状即正确，这条删掉。那条冒号守卫钉的是教程口径唯一，钉不了平台行为。
- (j) 跳过痕迹与遗留键（A12、A13）。① 的那两半里，"正文只有短理由、时刻只住在悬停提示里"已经本地验了
  （`--measure` 每一趟读回 `.skip` 的 `textContent` 与 `title`，正文出现 `\d{1,2}:\d{2}` 就红，
  对照 P4、P5）。真机剩的是现场：开着"尊重你的操作"，把某任务页摆在当前窗口前台等它到点，那一行末要
  真出现"上次跳过：你在操作"；全局暂停与自动暂停这两种情形下它不该出现（压制判据在
  `skip-trace.test.mjs`，场景里两种都不出现）。② 从 2.1.0 升上来，或在
  `chrome.storage.local` 里手动留一个 2.1.0 那版写过的 `cookieBackupHosts` 键，且备份开关开着：重启扩展
  之后各站点原有备份要原样还在、恢复登录照旧生效，那个遗留键不参与判断也不算存档。这一条错起来的表现是
  "重启后不再恢复登录"，是丢数据不是变慢，而升级路径在真机上只走一次。
- (k) 恢复进行中就点弹窗（A16）。开两三个任务、重启浏览器、趁标签页还在加载就点一次弹窗，任务一条都不许
  少。`prune` 那 1500 毫秒与真实的会话恢复不是同一个时间尺度，桩件给不了这个时序。反向的一半也只能真机看：
  确有一张被监控的页在这个会话里不会回来了（例如上个会话末尾关掉的窗口里的任务），等恢复收尾之后再开一次
  弹窗，要看到一条"已停止"的系统通知，而不是什么都不说就少一条任务。

既有缺口不变：开几个任务后重启浏览器或重新加载扩展，任务要全挂回原页面、不多开标签页、弹窗不卡住，且
DevTools 背景页的 Alarms 里 `refresh-<id>` 与 `hb-<id>` 同时存在（缺 `hb-` 说明心跳又被排到写盘前面）。

## 审计中推翻的结论（留档，避免被重新报一遍）

- "三处 `executeScript` 落在 MAIN world，保活脚本的 `chrome.runtime` 通道断了，页面可伪造活动"。假。
  `chrome.scripting.executeScript` 默认 `world: "ISOLATED"`，`content/keepalive.js` 的守卫与消息通道都在
  隔离世界正常可用。相邻的真实事实只有一个：合成事件带 `isTrusted: false`，站点看得见我们注入过。
- "`logic.test.mjs` 的抖动范围断言 `hi <= 60000 * 1.5 + 1 && hi >= 60000` 是恒真断言"。假。去掉 `pct`
  上限夹取后 `hi` 会变 114000，这条会红，夹取实际是被钉住的。它只是范围宽，不是空跑。
- "`alarm-gate` 的焦点用例没做对照"。假，而且做过了（那个文件末尾的两处）。问题不在断言松，在桩件把
  `getLastFocused` 建模反了。对应条目已修完移进 `CHANGELOG.md` 的 `[未发布]`。
- 同一份弹窗侧报告报回来的另外三条，逐条在本仓库重跑过：`sortTasks` 这个函数根本不存在；
  "`getTaskKeywords` 丢掉旧单串 `keyword` 的兼容读"红 2 条，有门禁；"`looksLikeLoginPage` 恒返回 false"
  红 4 条，其中一条是 `outbound.test.mjs` 的"两个出口一共只发出 0 笔，下面的断言会空跑"。三条都不是账。

## A 账索引

四十三条全部结案。每一轮的实测红→绿表在对应门禁文件末尾，改动与结论在 `CHANGELOG.md` 的 `[未发布]` 段。
编号不复用。

| 编号 | 轮次 | 那一格原本零判据的是什么 | 门禁 |
| --- | --- | --- | --- |
| A1 | 一 | 焦点窗口三态被压成两态，用户走开期间每次都判成"正在看"，从此永不刷新 | `alarm-gate` |
| A2 | 一 | 公共后缀（`co.nz` 那类）被当成注册域，备份与探针越界采到别家站点的票据 | `logic` |
| A3 | 一 | 关键词与验证墙检测只读顶层框架，整页挑战嵌在 iframe 里检不到 | `frame-scan` |
| A4 | 一 | 外发载荷带完整 query，一次性令牌与会话凭据跟着发出机器 | `outbound` |
| A5 | 一 | 弹窗不回填任务级字段，停掉再重开时原关键词静默消失 | `popup-repopulate` |
| A6 | 一 | 共享桩件十二条偏离真实 Chrome，多条门禁实际空跑 | 各文件末尾 |
| A7 | 二 | `settings` 两条无锁读-改-写互相覆盖 | `settings-cache` |
| A8 | 二 | webhook 失败无痕，且没有与微信对等的"发送测试" | `outbound` |
| A9 | 二 | `onMessage` 不认来源、设置键无白名单 | `message-gate` |
| A10 | 二 | 微信教程文案自相矛盾：凭据三项还是四项、中英两种冒号 | `wechat-copy` |
| A11 | 二 | 弹窗文本框只在失焦写盘，关窗即丢输入；测试按钮无 `try/finally` | `popup-save-timing` |
| A12 | 二 | SKIP 说不出被谁挡下；备份又多出一份主机名清单 | `skip-trace` |
| A13 | 三 | A12 那半自己引入的回归：清单漏登记一条，那份明文存档永久隐身 | `cookie-backup` |
| A14 | 三 | `task.url` 被登录页改写之后监控对象自指，掉线检测把自己 disarm | `task-url` |
| A15 | 三 | `startTask` 在任务锁里 `await` 外发，别的标签页连"开始"都要排队 | `start-task-lock` |
| A16 | 三 | 会话恢复晚到的那几秒里，弹窗的清理网把任务当"再也不会回来"删掉 | `prune-now` |
| A17 | 三 | 跨轮存活的连击计数与跳过痕迹没清，阈值被悄悄调低 | `rt-lifecycle` |
| A18 | 三 | 弹窗设置回流不重绘控件，另一台设备的改动被按打开那一刻的 DOM 翻回去 | `popup-settings-sync` |
| A19 | 四 | `fmtClock` 与 `wechatStatus` 的缺失分支从没被执行过 | `skip-trace` |
| A20 | 四 | 停任务时那句 `keepalive-off` 没有任何用例钉住 | `keepalive-channel` |
| A21 | 四 | 停止任务时该页在飞的关键词检测链没作废 | `detect-chain` |
| A22 | 四 | 两份 workflow 每次拿 `validate.mjs` 当闸门，而闸门自己没有测试 | `validate-refs` |
| A23 | 五 | README 那两张截图不是幂等产物，也没有再生成路径可对账 | `screenshot-mock` |
| A24 | 六 | `AGENTS.md` 与 `README.md` 里的名字、路径、行号对不对得上源码 | `doc-anchors` |
| A25 | 七 | 同一批文档里写死的数字对不上代码常量 | `doc-numbers` |
| A26 | 八 | `msg()` 别名包装传一个语言包里没有的键，一路 exit 0 过 CI | `validate-refs` |
| A27 | 九 | 占位符的位数没人核对，"键在不在"是另一根轴 | `validate-refs` |
| A28 | 十 | 弹窗那条 600px 上限只有"人眼看图"一道检查 | `screenshot-popup` |
| A29 | 十一 | 渲染那条通道页面报错不判红；按 id 取控件只对一个函数有账 | `screenshot-mock` |
| A30 | 十二 | 权限那行清单只记名字，源码调用点与桩件两头没对过 | `permission-map` |
| A31 | 十三 | 设置这本账五段（默认值、载荷、格子、读写点、事件清单）中间三段零判据 | `settings-map` |
| A32 | 十四 | 一条消息的类型名、载荷键、应答字段三处名字从没对过 | `message-ledger` |
| A33 | 十五 | 键名住在表里、经变量取用，正向反向两头都看不见 | `i18n-indirection` |
| A34 | 十六 | 微信卡片 20 字预算：标签装得下而站点没了，这一类红不出来 | `wechat-budget` |
| A35 | 十七 | 存储这本账：源码读写哪些键、落在哪个区，与清单两头没对过 | `storage-map` |
| A36 | 十八 | cookie 存档字段名散在三处手抄，改写侧一个字节无人红 | `cookie-schema` |
| A37 | 十九 | 角标五态的优先级与取值是两条嵌套三元式，四类改法全绿；本轮抽成 `decideBadge` | `badge-state` |
| A38 | 二十 | 角标那四件事实怎么取：归约看到第一个任务就停，探针键取整页主机名 | `badge-facts` |
| A39 | 二十一 | `doc-anchors` 判符号存在用的是子串，少写一个字母反而"存在" | `doc-anchors` |
| A40 | 二十二 | 验证墙那两条正则的清单本身没有反向账 | `wall-list` |
| A41 | 二十三 | class 名字的三本账没对过，正向当场点名一处真死类并删掉 | `class-ledger` |
| A42 | 二十四 | 人从弹窗之外起停任务的通道，名字与数字写在五处没对过 | `entry-names` |
| A43 | 二十五 | 那两份 workflow 整面零判据；glob 漂一个字母时 CI 对着空集合绿过去 | `pipeline-ledger` |

## 收口

A 系列到 A43 清账，2026-09-20 起停在这里，不再自开新一轮。理由是数得出来的：第二十一到二十五轮改的
产品代码是零，补的全是门禁，而上一回抓到真 bug 是 A41 那一轮删掉的一个死类名。再挖的边际收益低于成本，
且上面"还在账上的"那几行全都要人在场。下一轮真要开，先答得出这一轮打算改哪一行产品代码。

对照矩阵跑在仓库外整仓副本（`D:/Github/_tar_ctl_rNN/`）：`validate.mjs` 会遍历仓库根，副本放进来会被
当成待检文件。表里区分两种结果：改前零红是本轮补上的真空白，改前已经有别的用例红的是补账不是补覆盖面。
第二十五轮另有几类变异候选设计完没有跑（needle 逐个核过），落点与理由记在
`tests/tab-auto-refresh/pipeline-ledger.test.mjs` 末尾，别当成缺口重报一遍。
