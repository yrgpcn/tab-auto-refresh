# BACKLOG.md

待办清单，不是项目记忆。项目约定与"为什么这样设计"看 `AGENTS.md`，已发生的事看 `CHANGELOG.md`。

清单来源：2026-09-19 对 2.1.0 代码的一次从零审计（不采信文档结论，逐条对着源码确认）。
每条都带**源码位置**和**为什么现在测不出来**，因为这两样是动手时真正要用的信息；
修完任何一条，按 `alarm-gate.test.mjs` 与 `settings-cache.test.mjs` 末尾记的做法做一次红→绿对照，
"没有对照"就等于这条还没修。

维护方式：修完不删条目，整段移进 `CHANGELOG.md` 对应版本，本文件只留还在账上的。
新增条目同样要带源码位置，不接受"某处可能有问题"这种形状。
源码位置一律写符号名（函数名、消息类型、存储键）而不写行号——行号每动一轮就漂一次，
2026-09-19 的 A7 那轮之后本文件所有位置已统一改成符号名；A24 又抓出五处漏网的（其中两处是
当天新写进去的），并把这条规矩做成了门禁：`tests/tab-auto-refresh/doc-anchors.test.mjs`
同时扫 `AGENTS.md` / `README.md` / 本文件与 `CHANGELOG.md` 的 `[未发布]` 段，
两种行号锚形状都会红，已发布的历史段落除外。

2026-09-19 当日累计：A1（焦点三态）、A2（公共后缀越界采集）、A3（检测只覆盖顶层框架）、
A4（外发载荷带 query、凭据暴露面）、A5（弹窗不回填任务级字段）、A6（桩件十二条全部）、
A7（`settings` 两条无锁读-改-写互相覆盖）、A8（webhook 失败无痕、没有"发送测试"）、
A9（`onMessage` 不认来源、设置键无白名单）、E1（本机 node 门禁）已修完并移进 `CHANGELOG.md`
的 `[未发布]`，编号不复用。
A6 第 8 条欠的 cookie 执行器用例随 A2 一起交付（`tests/tab-auto-refresh/cookie-backup.test.mjs` 13 条）；
A5 让 popup 第一次有了门禁（`popup-repopulate.test.mjs` 10 条，切源码跑）；
A7 给 `settings-cache.test.mjs` 补了 4 条（并发覆盖、读前失效、锁方向、锁内"没变就不写"）；
A9 新增 `message-gate.test.mjs` 9 条（来源守卫逐类型、零副作用指纹、白名单端到端与纯函数、两条扫源码）；
A3 新增 `frame-scan.test.mjs` 27 条（多框架聚合与逐框架判定的纯函数层、切 `captchaProbe` 真实源码跑的
注入体层、两层接缝、执行器接线），并改动了 `detect-chain.test.mjs` 的验证墙桩件形状（布尔 → 事实对象）；
A4 剪枝落在 `notifyOut` 一处（不是三个调用点），`outbound.test.mjs` 补 4 条事件级用例并把全文件通用的
任务网址换成带令牌的形状、`logic.test.mjs` 补 `outboundUrl` 1 条、`popup-repopulate.test.mjs` 补 1 条
扫 `popup.html` 的守卫，七处对照记在两个文件末尾。
A8 两个出口对齐：`outbound.test.mjs` 补 10 条（留痕形状、覆盖、正文里的拒绝、逐码归桶、不留痕的两种、
两出口互不干扰、"发送测试"的边界）、`logic.test.mjs` 补 5 条 `webhookResultOf` 纯函数用例、
`popup-repopulate.test.mjs` 补 10 条（切 `renderWebhook` 真实源码 + 扫 `popup.html` 控件与 `popup.css` 颜色类）、
`message-gate.test.mjs` 补 1 条反向登记守卫（分发链每条弹窗专用分支都必须在 `POPUP_ONLY` 里），
23 处对照记在这四个文件末尾，其中两处第一次跑是绿的、补了输入形状才红。
A10 是纯文案改动：中英两份各 6 个键统一到"页面抄三项 + 建模板得第四项"，`wechatErrTemplate` 的
示范补上「关键词：中文冒号」前缀，`guideFixTemplate` 那条未实测的"换成英文冒号"兜底建议被删掉；
新增 `wechat-copy.test.mjs` 12 条（三条规则的数都从代码现推），18 处对照里 17 红、1 处是
"改一条守卫不该管的文案必须仍绿"的反向对照，其中 K10 第一次跑是绿的（英文句子没被当子句边界）。
A11 改的是弹窗侧的写盘时机：五个走 settings 的文本框从"只绑 change"换成 input 去抖 500ms +
change 即时 + 关窗补一次，微信「发送测试」补上 `try/finally`；新增 `popup-save-timing.test.mjs`
16 条（四个函数接假时钟与写盘计数器真跑），19 处对照 17 红 + 2 反向绿。有一处限制记在那个文件末尾：
"去抖时长改成 0"只有常量守卫抓得到，行为用例全绿（0 毫秒的 setTimeout 仍然推迟一拍）。
A12 两条一起交付。第 1 条：SKIP 的四种理由连同时间戳写进会话态 `rt:skip:<tabId>`，弹窗任务行显示
"上一次到点被跳过：某原因"，采集四条而显示两条（全局暂停与自动暂停行内已有角标，时间戳进 `title`
以保住 400×600 预算）；新增 `skip-trace.test.mjs` 20 条，22 处对照 19 红 + 3 反向绿。
第 2 条：备份读出 `cookieBackupHosts` 索引，三处 `get(null)` 换成"索引在位就定向读、缺失才退回
一次全量读并顺手补建"，判据抽成 `planBackupFetch` / `planBackupIndex` 两个纯函数；
`logic.test.mjs` +5、`cookie-backup.test.mjs` +6，14 处对照 11 红 + 3 反向绿，另有一处对照**零红**，
据此删掉了 `prune()` 收敛处那句摘索引的写（同一次启动末尾的清理必然再对一次账，改坏也没有症状）。
A 系列第一批十二条至此清账；当天在其后又跑了两轮审计，新增条目与它们的结案叙述都在下面的表与表后段落里。

## 优先级一览

| 编号 | 优先级 | 一句话 | 状态 |
| --- | --- | --- | --- |
| V1 | — | 2.1.0 真机手工验证（含下面几条只能真机验的检查） | 待做 |
| E2 | — | `_code-review/` 门禁是否迁入入库路径（A10 已照此把 `verify-wechat-template-doc` 换成入库的 `wechat-copy.test.mjs`，剩下的按这个形状挑） | 待定 |

A1~A12 十二条全部移进 `CHANGELOG.md`。2026-09-19 当天在 A12 落地之后的代码上又跑了一轮审计（同样不采信文档、
逐条对着源码确认，怀疑点用真 `background.js` + 共享桩件跑出来，或按红→绿对照做变异），新增 A13~A17 与 E3
（E3 同日结案，见"工程账"那一节）；
之后另有一趟面向弹窗与外发链路的审计，报回来的每一条都由本仓库自己重跑过变异再记账，收下 A18~A19、
否掉三条（那三条记在下面的留档段里，别被同一份报告再报一遍）。
第四轮审的不是插件而是那条打包链路（两份 workflow 加 `scripts/validate.mjs`），账记在 A22。
第五轮审的是 `README.md` 与两张截图对源码的漂移（同样零门禁的那一面），账记在 A23。
第六轮审的是这本账自己——`AGENTS.md` 与 `README.md` 里写的名字、路径、行号对不对得上源码，
账记在 A24。
第七轮接着核同一批文档里写死的数字对不上代码常量吗，账记在 A25。
第八轮回回到闸门自己：`validate.mjs` 那句"引用完整性"漏了别名这一条通道，账记在 A26。
第九轮顺着同一条文案链往下：键都在，没人核对过占位符要填几位，账记在 A27。
第十轮换一根轴：弹窗那条 600px 上限从来只有"人眼看图"这一道检查，账记在 A28。
第十一轮审的还是"把弹窗渲染一遍"这条通道：id 对账只管一个函数、页面报错从不判红，账记在 A29。
第十二轮换一根轴，审的是权限这本账：那行清单只记了名字，源码调用点与共享桩件两头从来没对过账，
账记在 A30。
第十三轮接着换一本账，审的是设置：`DEFAULT_SETTINGS` 的键、弹窗发出去的载荷键、页面上能填值的格子、
源码里的读写点、共用的事件清单——五段里中间三段零判据，账记在 A31。

A13（A12 第 2 条自己引入的回归）当日结案，走的是"不引入第二个真相来源"那一支：`cookieBackupHosts` 连同
`planBackupFetch` / `planBackupIndex` 一并删除，存档枚举只剩 `readBackupEntries()` 这一次 `get(null)`，
一趟启动的全量读从三次并成两次。判据落在用例里而不是注释里：6 条索引用例换成 5 条"先造清单外的孤儿、
再证明它被删掉"（启动 / 关开关 / 停任务三条路径 + 一条扫源码的守卫，因为"登记写回"没有读侧症状）。
变异对照 5 处：把删除依据换回清单可红 9 条（同一天上午这批门禁对着这个方向是一条都不红地全绿），
前缀过滤可红 15 条，关闭开关只查不删可红 2 条，登记写回只红源码扫描那一条；三处反向绿。
另有一处**零红**并如实记下：恢复侧只枚举被监控根域——不在根域里的存档既不会被恢复、下游也必然被清理删掉，
两个分支同结果，与上一批的 P8 同类，不为此改代码也不为此补断言。

A14 同日结案，走的是"登录页不参与改写"那一支：`task.url` 的语义定死为**用户指定的监控对象**，
判据抽成 `shared/logic.js` 的纯函数 `shouldAdoptTaskUrl(taskUrl, tabUrl)`（同站 **且** 新地址不是登录页才跟随），
`refreshTaskUrl` 只剩执行。没有另加"用户填的原始目标"字段——多一个键就多一处要跟 `prune` 的
挂接/重映射/补记对齐的地方，而"监控对象永不完全等于登录页"这一条本身就堵死了自指。
门禁的现场形状是这次的关键：页面**停在** `/login`、`complete` 事件不带 `url`，后台只能从 `tabs.get` 读到；
原先那条行为通道用例给的是 `changeInfo.url` 带地址、标签页仍停在业务页，正好绕开自指。
新增 `tests/tab-auto-refresh/task-url.test.mjs` 4 条 + `logic.test.mjs` 1 条纯函数表。
对照 7 处在仓库外整份副本上实跑（副本里 `tests/` 与 `tab-auto-refresh/` 同级，一次改坏纯函数与 e2e 两头都看得见）：
改回 A14 之前的形状红 2、去掉同站约束红 2、改过头成"永不跟随"红 2、确认窗口降成 1 次红 2、
去掉"网址没变就不写盘"红 1、畸形网址放行红 1，另有一处不在这条路上的对照只红在既有用例上。零红的一条也没有。
已受损记录（旧版本已经改写成 `/login` 的任务）不需要迁移：用户登录成功、页面离开登录路径的那一刻就跟随更新回去，
这条有专门用例盯着。残余边界如实记下——用户从此不再登录，任务就一直监控着那张登录页，
行为通道对它照旧豁免（那是"用户故意监控登录页"该有的语义，不为它加特例）。

A15 同日结案：`startTask` 的首次备份从任务锁里挪出来、排到整条锁内流程之后，`armRefresh` 与
`ensureHeartbeat` 提到紧跟 `setTasks` 的位置——中间不再夹任何可失败的等待。**仍然 await 那笔外发**，
没有改成裸甩（裸甩丢的正是"会话掉线"这条通知，这一点由 N3 那处对照钉着）。
新增 `tests/tab-auto-refresh/start-task-lock.test.mjs` 4 条，靠的是在 boot 之后把 `globalThis.fetch`
换成永不 resolve 的桩——E3 说的"桩件没有 Promise 形态的应答"这条限制，当时是在单条用例里绕过去的；
那处覆写已在 E3 结案时收回桩件。对照 6 处：A15 原样退回红 2、备份挪到锁之前无条件执行红 3、改成裸甩红 1、
心跳排到写盘之前红 2（连带既有的「右键开始 1 分钟」）、先挂表再验现场红 1。
另有一处**零红**顺手量出来并记成新账：删掉 `stopTask` 里的 `stopKeepAlive(tabId)` 全套 333 条一条都不红，
也就是 A20。它不是 A15 的改坏目标，但同一把尺子量到的缺口没有装看不见的道理。

A16 同日结案：`cleanupInvalidTasks` 的两条判据分开了——"这一页取不到"与"这一页再也不会回来"。
本轮启动恢复没收尾就一条都不动（交回 `prune` 的认领窗口收尾），收尾信号是 `prune` 整套动作跑完后
写进会话态的 `rt:pruneDone`；真删时逐条 `stopTask` 全部停完再统一发 `task-stopped` 通知，
不再静默消失。通知排在一起而不是"停一条发一条"，是因为 `notifyTaskStopped` 里 await 的是 fetch，
挂住的网络会把后面几条任务的停止一起堵在门外——与 A15 同一条纪律，只是这处的状态收敛是删任务。
新增 `tests/tab-auto-refresh/prune-now.test.mjs` 7 条：未收尾一条都不动、prune 正在跑时开弹窗也不动
（钉收尾标记写在 prune 末尾这个位置）、收尾后真删要发通知、外发载荷带对网址与理由、
外发挂住时全部任务照样当场停完、收尾标记活过 SW 回收（同一个 env 上再 boot 一个新实例）、
以及标签页都在场时一条都不停的反向守卫。走"已收尾"的用例不手抄键名，靠真跑一遍 `prune` 拿到标记。
对照 6 处改坏加 1 处对照在仓库外副本实跑，pristine 340 全绿：去掉判据红 2、不发通知红 4、
停一条发一条红 1、通知不带网址红 1、标记挪到 prune 开头红 1、标记改成内存变量红 6；
对照那处（删掉清理网末尾的 `updateBadge`）红 2，专门验两处"本用例是空跑"的哨兵不是在装饰。
这批没有零红项。

A17 同日结案：会话态里按 tabId 存的键有了两份清单，`rtRoundKeys(tabId)` 是"上一轮的结论"
（`rt:error` + `rt:captcha` + `rt:skip`），`rtTabKeys(tabId)` = `rt:activity` + 上一份。
三处清理时机对齐到这两份清单上：`stopTask` 与 `reopenTaskTab` 用全量那份（旧 id 整批拆掉），
`startTask` 用结论那份（同一个 id 上重新开始就是不经过 `stopTask` 的那条路，此前它清了三条通知
却没清三个计数，验证墙阈值从三次悄悄变成一次）。`rt:activity` 刻意留在作废清单之外——它是
"用户最后一次真在这个页面上操作"的事实而不是结论，跟着清会让下一拍刷到用户眼前，它自己按
`ACTIVITY_SKIP_MS` 过期。`probeCaptcha` 在守卫关闭那一支先复位再 `return`：关着的这段时间里页面
正常加载过若干次，重新打开守卫不该从残留值往上加。`reopenTaskTab` 那句删除排在 `setTasks` 之前，
因为 `setTasks → armRefresh` 之间那段是 A15 记下的"写完紧接着挂"的窗口，不往里添新的等待。
新增 `tests/tab-auto-refresh/rt-lifecycle.test.mjs` 10 条：停任务清整批、同 id 重新开始作废、
`rt:activity` 不跟着清的反向守卫、重新开始后验证墙仍要数满三次、守卫关闭时复位、关掉再打开守卫后
第一次命中不算第三次、重开标签页拆旧 id 且新 id 干净、只搬一张页时别的标签页不受影响的反向守卫、
一条种子哨兵、一条把清单取出来求值再与字面量键对账的守卫。9 处改坏在仓库外副本实跑，
pristine 350 全绿、**没有零红项**：删掉 startTask 的作废红 2、清单漏 captcha 红 5、把活动戳也清掉红 3、
全量清单退回手写且与结论清单分叉红 3、守卫关闭不复位红 2、reopenTaskTab 不拆旧 id 红 1、
改成 `session.clear()` 红 1、新增一族键没进清单红 1、行为不变但退回手写数组红 1。
这批自己也被抓了一次：清单守卫第一版只按正则扫 `rtTabKeys` 那一行有没有出现 `RT_ACTIVITY` 这个名字，
于是我写的 `rtTabKeys` 漏拼 `:tabId`（删的是根本不存在的 `"rt:activity"`）从它眼皮底下过掉，
是行为用例先红的——改成求值再对账之后这类形状才拦得住，记在同一个文件末尾。

A18 同日结案：弹窗的 15 个设置控件有了唯一的填充函数 `populateSettingsFields()`，`init` 与
`storage.onChanged` 的 `settings` 分支各调一次，回流那一次排在 `refreshState()` 之后（读的是刚
重新归一化过的 `settings`，默认值合并与 1.7.0 `webhookEvents` 迁移都在 `normalizeStoredSettings` 里，
直接吃 `changes.newValue` 会绕过它们）。约束①按"共用同一把判据"落地：五个走 settings 的文本框在
获得焦点、或 `saveTimer` 还挂着（A11 的去抖那一拍）时**整组**跳过——不逐框判断，因为凭据四项是
一把钥匙的四段，填三段留一段会拼出一个两边都不认识的组合，而它马上被那笔待写的盘提交；
复选框没有输入中间态（`change` 即时写盘），照常同步。约束②原样保留：`saveSettings` 仍发整份快照，
所以这次修的是"撤销窗口"的长度（从"弹窗开着多久"缩到"一次往返"），不是竞态本身，那条注释里
写着的重新论证要求没被绕开。顺带把 `body.wx-mode` 一起收敛了：远程关掉微信直连就退出那一页，
反向刻意不做（远程打开不该把用户从当前页面上拽走）。
新增 `tests/tab-auto-refresh/popup-settings-sync.test.mjs` 14 条：同步函数、`wechatStatus`、
两个渲染、`storage.onChanged` 那个箭头监听器块全切真实源码跑，监听器那条喂四种回流断言整条链的
顺序与不触发；另有一条"清单同源"守卫，从 `saveSettings` 源码认出它读的 18 个控件与同步函数写的
18 个求对称差，以后新增设置项只写进一头就红。15 处改坏实跑（变异全在 popup.js 文本里，
`TAR_POPUP_SRC` 指过去即可，不必整份复制仓库）pristine 364 全绿、**没有零红项**：红 1~10 条不等，
最多的是"同步函数末尾顺手写一次盘"红 10。其中一处第一次只红 1 条——假 DOM 的复选框初值全勾着、
用例喂的又是全真的 `settings`，"复选框照同步"成了 true 比 true，改成喂反方向的值之后红 3 条，
与 A5/A8 记的"桩件比真实输入更干净"是同一类，记在同一个文件末尾。
这批还顺手关掉 A19 的前半：`renderWechat` 现在被门禁按真实源码执行（探针重跑：把它改成立刻
`return` → 红 1 条），而 `fmtClock` 那一半在 364 条下仍然零红，A19 因此缩成只剩 `fmtClock`。

A19 同日结案，**产品代码一个字没改**——那两个函数今天的实现都是对的，欠的只是能钉住它的用例。
① `fmtClock` 真身切进 `skip-trace.test.mjs` 当第 5 层跑（该文件原先 9 条名字里带 fmtClock 的用例
把它当**形参**注了个假的 `(ms) => "T" + ms`，真身一次也没被调用），另在 `popup-settings-sync.test.mjs`
把真身注进同一次"回填 → 渲染"链。钉的是**数字分组的形状**（正好两组、每组两位）而不是 `"09:07"`
这种字面量——本机 zh-CN + Asia/Hong_Kong 得到 `09:07`、CI en-US + UTC 得到 `09:07 AM`，两种都得过关；
时刻按本地分量构造，换时区不必改任何用例。② `wechatStatus` 的"凭据不全 → 点名缺哪几项"补 4 条，
含一条正向的"配齐时报的是投递结果、不是缺失"——没有那条，判据退化成恒报缺失也不会红。
13 处对照（F1~F6 打时刻、W1~W5 打缺失分支、RF1/RW1 两处反向必须全绿）在仓库外副本实跑，
每个用例对两个门禁文件分开各跑一遍以保住归因，**没有零红项**：A19 那条原探针（`fmtClock` 改成
立刻 `return ""`）从 364 条零红变成红 2+1 条；两处第一次为绿的判读也如实记下——"坏时刻不许抛"
只有把 try/catch 一起去掉才红（F6，光写坏选项会被 catch 兜住），正向那条只有把判据整个反过来才红（W5）。
`skip-trace` 20 → 25 条、`popup-settings-sync` 14 → 18 条，全套 373 条全绿。
探针顺手排掉的三个假警报挪到下面的留档段里了。

A20 同日结案，**产品代码一个字没改**：那句 `keepalive-off` 一直发得出去，欠的是能钉住它的用例，
而且缺的不止停任务那一头——注入侧与 `reconcileKeepAlive` 整个函数同样零覆盖，
`content/keepalive.js` 这个文件在门禁里从来没被执行过（只有 `message-gate` 扫过它的源码文本）。
补的是新文件 `tests/tab-auto-refresh/keepalive-channel.test.mjs`，18 条分三层，缺一层都留静默缺口：
① 后台侧真跑 `background.js`——注入的三种开关组合（含"只开活动监听也要注入""两个都关就不注入但仍推
'都别跑'那份配置"）、注入必须排在推送之前（脚本正是这次调用注进去的，推送早于注入时页面上还没有监听器，
两笔调用并进一条流水才断言得出顺序）、每次加载完成各补一次、非监控页一次都不注、
注入被站点整次拒绝时任务照建（那一拍在 `withTaskLock` 里 await 着，抛出来就是"点开始没反应"）、
停任务只向那一页发 off、早退排在发 off 之前、重开时旧页收 off 新页重新注入、
关掉开关时 `reconcile` 发 off 而不是继续注入（页面不重新加载，`onUpdated` 那张网永远不来）、
无关设置键变了不惊动任何任务页；② 页面侧把 `content/keepalive.js` 整份源码在假
window / document / chrome 上执行一遍，config 真的起心跳（跑一拍要真派发三个事件）、
off 真的全停（定时器清、监听摘、全局钩子删）、两项起停互不连坐、重复注入可重启、
真人事件才上报 `user-activity`；③ 接缝一条：拿后台真回的那份查询应答与真发出去的那两条载荷
喂给页面侧真监听器——类型名只改一头，两侧各自的用例都还会绿，这条不会。
17 处对照（九处后台、七处页面侧、一处反向）在仓库外副本实跑，红名单记在文件末尾，
**背景侧没有零红项**。两处如实记下的判读：一处页面侧改法零红是**等价变异**（下一行又把活动监听
起回来了，净结果与原文一样，换成非等价的那一种即红），挑对照要挑净结果变了的那一种；
反向那一处（删掉 `detectChains.delete`）本该只有 detect-chain 那头红，实测**那头也零红**，
于是新开 A21。全套 373 → 391 条全绿。对照入口除既有的 `TAR_BG` 之外新增 `TAR_KEEPALIVE`
（页面侧那批用例按文本读源码再执行，换文件才能改坏它）。

A21 同日结案，**同样一个字产品代码没改**——`stopTask` 里那句 `detectChains.delete(tabId);` 一直在，
欠的还是用例。补进 `detect-chain.test.mjs` 三条（8 → 11 条）：一条正向见证（挂起注入、不停任务、
放开 → 命中通知照发），两条负向（停任务之后在飞的那一次采样不许再发 `keyword-hit`；
continue 模式下停掉再以新关键词重开同一张页，旧链不许把上一轮的在场集写进新任务的 `notifiedKeys`）。
造法只有"把首拍那一次注入挂住"这一条——三次采样是 `[0, 3000, 10000]` 真 `setTimeout`，
用例等不到第二拍，也不需要假时钟。四处对照在同一套仓库外副本实跑，落点全部符合预期：
删掉那一行只红点名的两条（正向见证照旧绿）；删掉注入回来之后那道 token 判据只红一条，
另一条不红不是缺口，它防的写入支路另有 `notifiedKeys` 回写前那道 `detectChains.get(tabId) === token`
（在 `withTaskLock` 里）兜着（两条判据各挡一半现场）；
把首拍的 `0` 去掉红 5 条含正向见证——这一处是给"见证"本身做的对照，链没跑到时它必须响；
反向那处（删掉 `stopKeepAlive`）本文件零红、`keepalive-channel.test.mjs` 红 2 条，
两头的分工是断出来的。全套 391 → 394 条全绿。**A 系列第一批至此（A1~A21）清账**；当天在其后
又跑了第四到第十四轮审计（第四轮审校验闸门、第五轮审 README 与截图、第六轮审项目记忆本身、
第七轮审文档里的数字，第八轮回到那道闸门、补它漏掉的一条引用通道，第九轮顺着同一条文案链补它
漏掉的另一根轴，第十轮量的是弹窗那条 600px 红线——至今没有一个像素级的判据看着它，
第十一轮审的还是那条渲染出来的通道：弹窗按 id 取控件只对账了一个函数，而页面报错从不判红，
第十二轮审的是权限那行清单到底记了什么：名字与 manifest 对得上，调用点与桩件两头却从没对过账，
第十三轮审的是设置这本账：默认值、弹窗载荷、页面上的格子、源码读写点、事件清单，中间三段没人核过，
第十四轮审的还是"同一个名字在两个文件里各写一遍"这件事，换的是消息这条通道：一条消息的类型名、
请求载荷键、应答字段三段全跨文件，从弹窗那一头改坏没有一条测试会红，
都不是插件本体），
账记在下面十二段 A22 到 A32。十二段之后剩下的只有待定项 E2，以及只能真机验的 V1
（工程账 E3 已在同日结案，见下面"工程账"那一节）。

A22 同日结案，**插件产品代码一个字没改**——这一轮审的不是插件，是当时全仓库仅剩的几面零门禁之一：
`ci.yml` 与 `release.yml` 每次都用 `scripts/validate.mjs` 当闸门，而闸门自己没有任何测试。
（另两面是 `README.md` 与两张截图、以及 `AGENTS.md` 这本账对源码，紧跟着的 A23 与 A24 各审一面；
A25 顺着 A24 留下的那条边界，审同一批文档里写死的数字；A26 再回到闸门本身，把 A22 那句
"引用完整性"里当时没说全的那一条别名通道补上。）
两份 workflow 逐条核过（跑什么、tag 版本比对、`git archive` 的取件面、旧 Release 清理），没找到缺陷，
不在这里记账。账落在闸门那句"引用完整性"上：它**说的**比它**查的**多。

- 三条缺口都是量出来的，不是猜的。改前的 `validate.mjs` 对 `__MSG_` 只查 manifest 顶层的 `name`
  与 `description`，而 manifest 里实际有四处引用（另两条是 `action.default_title` 与
  `commands.toggle-refresh.description`）；HTML 的 `src`/`href` 一条也没查（真实 7 条，
  `popup.html` 与 `wechat-setup.html` 各几个 CSS/JS/图片）；语言包只有"两份键齐平"这一条判据，
  正向（源码引用了语言包里没有的键）与反向（写了却没人提到的孤儿）都没查——而这两种恰好是
  界面上静默少一块、和打错键名最常见的两种失效形状
- 按纪律 1 把判据抽成 `scripts/validate-refs.mjs` 的五个纯提取器（`manifestMsgKeys` 深走整份
  manifest、`htmlLocalRefs`、`htmlI18nKeys`、`jsMessageKeys`、`stringLiterals`），`validate.mjs`
  只剩遍历、拼路径、报账。抽出来的直接理由就是"正则要有单测"：执行器没有 fixture 也没有 DOM，
  能判的那一半必须能单独 import，否则这一面门永远只能靠读代码相信
- 新增 `tests/tab-auto-refresh/validate-refs.test.mjs` 12 条：8 条对着注入源码的正则形状
  （单双引号、query 剥离、三条 `data-i18n` 通道、只取第一个实参、嵌套调用不截断），
  4 条扫真实插件目录并给每条通道一个数量下限——下限不判对错，只挡"正则或目录结构变了之后
  对着空集合绿过去"
- 对照 14 处，全在仓库外整仓副本实跑（闸门跑的是真 `validate.mjs`，副本放进仓库里会被它自己的
  目录遍历当成待检文件）：改提取器 5 处、改产品文件端到端 5 处、把同一处破坏配回 HEAD 那份
  `validate.mjs` 4 处。后一组是**漏检证据**，改前 exit 0、改后 exit 1，缺的就是这条判据本来该有它
- 正向与反向各盖一半，这一点由两处对照量出来而不是写在注释里。V4 把 `popup.js` 的
  `msg("btnStart")` 打成 `btnStartt`：正向判据一个字也看不见（`msg` 是 `getMessage(变量)` 的别名，
  实参不是 `getMessage` 调用的字面量），只有反向那条红；G1 把 `manifestMsgKeys` 退回只看
  `name`/`description`：红的四条里包含"每条文案都有人提到"，因为 `commandToggleRefresh` 只有
  manifest 这一条通道能救。正向少收一处、反向立刻跟着红，这就是"两头一夹"
- 写这个闸门时撞到的真实假报警留在判据里：第一版对真实 `background.js` 报"引用了语言包里没有的键
  captcha"——`p.reason === "captcha"` 那个字面量是拿去比值的，不是键。`jsMessageKeys` 因此要先切
  三元分支（`branchesOf`），G4 就是这一处的对照，红 3 条
- V2b 那处只为确认新判据没把老判据挤掉：只往 `zh_CN` 加一条孤儿，`validate.mjs` 报两条，
  既有"两边键齐平"照红。反向判据认不出注释（注释里带引号写键名也算"提到"）、静态判不了拼出来的键名
  （本仓库现在零处，真出现会红在"没人提到"而不会静默），这两条边界连同执行器无单测一起写在测试文件末尾，
  免得被当成已覆盖
- 全套 405 → **417** 条全绿，`scripts/validate.mjs` 通过且当前零孤儿、零缺失引用；
  权限、默认值、存储键、消息类型一个都没动

A23 同日结案（第五轮），审的是**README 与两张截图对源码的漂移**——这一面原来同样零门禁：
没有任何测试读过 `README.md`，也没有任何东西核对过截图是不是当前代码渲染出来的。
插件产品代码只动了两条语言包的 `message` 文案（键没增删、默认值没动），其余全在文档、
工具与测试。

- 三条文案漂移，每条都是先读源码确认行为、再改话术：
  ① 验证墙。README 写"过墙后可手动恢复"，`notifTaskPausedCaptcha` 写"请在页面中通过验证后点恢复"，
  而 `probeCaptcha` 在每次页面加载完成探不到墙时就 `resumeTaskAuto`（`probeCaptcha` 未命中墙那一支，
  清掉连击计数后紧跟着的那句解除），这条自动解除本来就有正向用例
  （`detect-chain.test.mjs` 的「墙消失后自动解除暂停，暂停通知跟着收掉」）和删掉它必红的对照。
  **漂的只有文案，行为一直是对的**——通知让用户去点一个系统已经替他点好的按钮。
  ② 站点锁定那条没写 A14 之后的判据（跳去登录页时监控对象不变），补一句。
  ③ README 的技术栈行只列了七个扩展 API，权限清单有十条：漏的正是 `scripting` / `idle` / `power`
  三条背后那三个能力（关键词与验证墙注入、空闲重同步、防休眠）
- 截图那半边比文案严重：`popup.png` 是早先手工截的，图上没有 A12 那句"上次跳过：你在操作"，
  也没有弹窗回填关键词的那一行；`popup-wechat.png` **从来没有任何脚本写过它**，只能手工截一次，
  之后各自漂移。而重跑脚本也救不回来——它的 chrome mock 里压根没有 `storage.session` 这一块，
  弹窗每秒读的那次 `rt:skip:<tabId>` 在 mock 上必然落空，那句解释永远截不出来。
  现在脚本一次出两张，两份 PNG 都由它产出，mock 补齐了 session 区域与两个场景的投递结果
- 新增 `tests/tab-auto-refresh/screenshot-mock.test.mjs` 4 条，把"手抄副本"这件事钉成判据：
  弹窗用到的每一条 `chrome.<ns>.<member…>` 必须在 mock 里有同名实现（用面取到三层，
  只到 `storage.session` 那一层不算，弹窗读的是 `.get`）、README 引用的每张 PNG 必须由脚本写出、
  扫描本身不许对着空集合绿（正向见证）、每个场景必须给全 `DEFAULT_SETTINGS` 的每一个键
  （少给一个不报错，只会让那个开关在图上看着是关的）
- 对照 8 轮实跑（`base` 加 C1~C6 加 R1，副本在仓库外）：`base` 先证明"复制加重定向"本身全绿；
  C1 删掉 mock 的 session 整块红 1 条、点名 `storage.session.get`（就是改前的真实形状）；
  C2 改掉一个输出文件名红 1 条；C3 让第二个场景少一个设置键红 1 条；
  C4 弹窗新增一块 mock 不认识的存储区域红 2 条（见证加覆盖，同一条断链的两个侧面）；
  C5 弹窗改用一个 mock 没有的面红 1 条（证明 C4 那两条不是"改哪儿都连带两条"）；
  C6 只把方法名换掉、区域还在，红 1 条——这一根就是"取到三层"那条判据的来处，
  只切两层时它查不出来；R1 反向（mock 多给一条没人用的面）全绿，判据刻意单向。
  写这个闸门时撞到的两处扫描器自错记在测试文件末尾，不当成"守卫本来就松"
- 已知边界一并写在测试文件里：这条门禁比的是**有没有这一面**，不是**这一面回得对不对**。
  mock 少给一个 `tasks` 的字段、或 `runtime.sendMessage` 回错形状，截图照样出、门禁照样绿；
  设置那一面由"给全 `DEFAULT_SETTINGS` 键"那条兜住，任务与会话态的取值形状仍然只能靠人看图
- 全套 417 → **421** 条全绿，`scripts/validate.mjs` 通过（38 个 JS 文件）

A24 同日结案（第六轮），审的是**项目记忆与用户说明书对源码的漂移**：`AGENTS.md` 是下一个协作者
和下一轮审计读的那本账，里面写满了源码里的名字（函数、常量、存储键、消息类型）与文件路径，
而这一面同样零门禁——没有任何测试读过它。产品代码一个字没改。

- 先说这一面**对得上**的部分，因为"对得上"也是要记的账：`AGENTS.md` 反引号里的 262 个符号名
  与 55 条路径逐个在仓库里核到（脚本扫的，见下面那条门禁），人另外逐条核了机器核不了的：
  默认值与开关清单、`PAUSE_CONFIRM_SAMPLES` / `CAPTCHA_CONFIRM_SAMPLES` / `ACTIVITY_SKIP_MS` /
  `RECLAIM_WATCH_MS` 这些阈值的值、权限十条与 host 权限那条决定、语言包 180/180 键齐平、
  以及每条"由某某用例钉住"的说法是否真有那个文件与那些用例
- 一处真漂移，而且是**这本账自己的规矩被它自己破了**。文件开头"维护方式"那一节写着
  "源码位置一律写符号名而不写行号——行号每动一轮就漂一次"，而违反它的正是记规矩的这一段：
  一处按行号引用 `logic.test.mjs` 的那条抖动断言，行号早漂走了（断言现在在别处），另有两处写成
  "第 N 行"的中文锚指 `background.js` 里的 token 判据，加上 A23 当天自己新写进去的两处
  `file.ext:NNN` 形状。
  行号锚最坏的地方不是漂，是**漂了零症状**：它不报错，只是安静地指着别的一句话，
  而读者没有任何办法察觉。五处全部改成按名字锚（函数名 + 判据字面量），改完文件里 `file.ext:NNN`
  与"第 N 行"两种形状都是零条
- 纪律 1 的文档版：判据抽成纯函数（`classifyToken` / `docTokens` / `unresolvedPaths` /
  `unresolvedIdents` / `lineAnchors` / `unreleasedSection`），新增
  `tests/tab-auto-refresh/doc-anchors.test.mjs` 14 条。三条判据各配正向见证——前两条都是
  "集合为空即绿"的形状，所以先把数量下限钉住（符号名 ≥250、路径 ≥45、语料 ≥50 万字符、
  `[未发布]` 段 ≥8000 字），再加一条"真在用的那批名字逐个认得"，防止判据退化成恒报缺失或恒绿
- 分类器有两处是按踩过的坑写的，各有一条负向用例：`origin/main` 与 `try/finally` 都含斜杠，
  进了路径堆就永远"不存在"；tag 模板 `tab-auto-refresh/vX.Y.Z` 与下载文件名模板整条豁免，
  `_code-review/` 前缀也豁免（本地目录、不入库，拿它做存在性判断必假阳）
- 覆盖不对称的地方写清楚，不留"看起来全覆盖"的错觉：`第 N 行` 只禁 `BACKLOG.md` 与
  `CHANGELOG.md` 的 `[未发布]` 段，因为 `AGENTS.md` 与 `README.md` 用这句话指弹窗 UI 的第几行
  （"列表封顶 108px、第三行露头"那种版面描述），那是版面不是坐标；已发布的历史段落不设行号判据，
  那些行号是当时的事实，回头改它等于篡改账
- 对照 9 处在仓库外**整仓**副本上实跑（判据吃 `TAR_DOC_ROOT`；整仓而不是只拷文档，因为三条判据
  两头都吃仓库）：base 14 条全绿；改坏 `AGENTS.md` 里一个符号名红 2、路径打错一个字母红 1、
  删掉一个门禁文件红 1、`AGENTS.md` 塞回行号锚红 2、`BACKLOG.md` 塞回一处"第 N 行"锚红 1、
  `[未发布]` 段塞回行号锚红 1、把 `RECLAIM_WATCH_MS` 连同测试一起改名而记忆不动红 1（本轮要防的
  正是这一类）、`README.md` 路径改掉红 1、换 `decideWallFromFrames` 再跑一次改名红 1。
  逐条红名单与两处扫描器自错记在测试文件末尾
- **门禁当场抓住了记账本身**：上面那段 A24 的账写进 `BACKLOG.md` 之后先跑了一次全套，两条锚点
  判据立刻红——记"行号锚不该有"的那段话里，实例照原样抄了一遍（一处是 `logic.test.mjs` 那个
  失效引用，一处是那句"第 N 行"锚的原句）。改成描述形状而不是抄实例才绿。这不是段子，是这条
  判据真的在工作：它不分"引用它"和"违反它"
- **一批门禁自己的账**：C1 第一次跑只红那条硬编码正向见证，一般判据照绿——因为语料把**门禁
  自己那个文件**也算进去了，而它的注释里正写着 `planPruneV3`（就是对照清单里那一行）。
  判据在注释里提过一遍的名字从此永远算存在，这一面看着有门禁、实际是自证。排除自身之后 C1
  才红到一般判据。同一次还量出扩展名分支必须长名在前：`js|json` 那个次序会把 `manifest.json`
  截成 `manifest.js`，路径判据当场指错文件
- 全套 421 → **435** 条全绿，`scripts/validate.mjs` 通过（38 → 39 个 JS 文件）；
  默认值、权限、存储键、消息类型一个都没动。文档侧改了五处锚点与 A22/A23 两段"仅剩两面零门禁"
  的措辞（现在三面都关了）

A25 同日结案（第七轮），审的是**文档里的数字对代码常量**——A24 留下的那条边界就是这一轮的全部射程：
符号名门禁挡得住"整个仓库一个字都不剩"的改名，挡不住"名字还在、值已经变了"，而阈值、时长、上限
恰恰是数字在说话。产品代码一个字没改。

- **这一轮没抓到一处真漂移**，先说清楚，免得后面的门禁被当成"修了什么"。人肉逐条核之前先把两边
  量出来再对：文档侧 33 处候选数字（心跳 4 分钟、±15% 抖动、连击 2/3 次、6 小时节流、60 秒活动窗、
  200 条 / 30 天 / 20 站、100 字 10 个、12~20 与 45~75 秒、400×250、20 字、15 秒超时、5 分钟提前量、
  2000 字正文、1500 毫秒等待、0~60 秒打散、3 秒与 10 秒重采样、400/600/108px、Chrome 120、
  7 档预设、10 个开关格、四类通知、四项凭据、权限九项、默认开 5 / 默认关 4），代码侧 28 处常量与
  算式，**全部一致**。也就是说 A24 那次人肉核过的东西到今天是可信的。这一轮的产出是那道门：
  下一次改常量而忘了改文档，表现从"没人发现"变成"全套红一条并且说出是哪一行"
- 判据落进 `tests/tab-auto-refresh/doc-numbers.test.mjs`，45 条用例：36 行"文档里的正则 + 源码现推
  的期望值"，加取值见证、原语单测、两条反向见证。数字只有一个来源——源码文本：`arithConst` 只认
  整数字面量与四则算式（`30 * 24 * 60 * 60 * 1000` 这种按单位换算的也照算），单位换算是文档口径
  （天、小时、分钟、秒），不是第二份常量。**不 import 模块**：对照跑在仓库外的整仓副本上，静态
  import 解析不到副本根目录，那样一半的推导就没法被控制了
- 三条判据的形状与 A24 一致：取不到值就是红，绝不静默跳过。推导断了时 36 行**每一行**都写
  "取值失败，本行没比对过 → …"而不是集体消失（测试总数恒为 45 条，条数一变就看得出来），
  文档那句话改了措辞同样红——"一处都没匹配到"与"数字对不上"是同一类事故
- 射程比"标量对账"宽一点，因为同一次读盘就能顺手核：`AGENTS.md` 的权限清单与 `manifest.json` 的
  permissions 做集合相等；"当前默认开 / 默认关"两份清单与 `DEFAULT_SETTINGS` 的布尔值做集合相等
  （纪律 3 从此有机器可查的一半兜着）；`README.md` 那行"30 秒 / 1 分钟 / … / 1 小时"换成秒之后与
  `PRESETS` 做序列相等；数量词（四类通知、10 个开关格、四项凭据）由 `NOTIF_ID` 的键数、
  `check-grid` 里的 `<label>` 数、`wechat*` 字段数现推，中文数字词另有一条单测
- 对照 10 处在仓库外整仓副本实跑（判据吃 `TAR_NUM_ROOT`）：base 45 条全绿；改常量一处（心跳 4→5）
  只红那一行；改文档数字一处（±15%→±20%）只红那一行；源码里给 `MAX_COOKIES_PER_HOST` 改名 41 条红
  且每行报取值失败；加一个开关格、删一档预设、改列表 px、翻一个默认值、加一条权限、改心跳节奏各红
  1 条；最后一条是**数字一个没动、只把"列表封顶 108px"改成"列表限高 108px"**，红 2 条
  （那一行加"覆盖面见证"）——判据失去覆盖面本身就是要报的事
- 两处第一次不合格的记录：① 反向见证用例把"源码现推 4"抄成了字面量，于是改心跳常量会连带把见证
  用例改红，红名单里混进无关条目就看不出真正红了哪条，改成全部从推导值现推；② D3 一开始用"整仓
  改名"做对照，结果副本里连门禁自己的判据一起被改了，跑出来全绿——那是自证清白的语料不是对照，
  换成只在产品源码里改才红。同一次还撞到 `popup.html` 是 CRLF，判据用的 needle 里不能带换行
- 边界写在测试文件末尾：它只管"文档写的数等于源码算出的数"，不管"该不该是这个数"；不在代码里的
  平台事实（弹窗外框上限、SW 闲置回收、第三方验证框尺寸）不设判据；讲改动前行为的那句历史数字
  刻意不收。全套 435 → **480** 条全绿，`scripts/validate.mjs` 通过（39 → 40 个 JS 文件）

A26 同日结案（第八轮），**插件产品代码一个字没改**，抓的也不是活的错：弹窗与微信教程页经别名
`msg("键")` 用到的 48 条键逐个核过，全部在语言包里。这一轮补的是闸门里那条**从来没被写下来过的**
引用通道，A22 那句"引用完整性"到这里才算说全。

- 洞的形状是"两条判据同时看不见同一处"，这才是它能活到今天的原因。正向那条只认
  `chrome.i18n.getMessage(` 调用的第一个实参，而 popup.js 里唯一一次 getMessage 调用是
  `getMessage(key, subs)`——实参是变量，抽取器按规矩不收；那 48 处键全在别名 `msg(...)` 里。
  反向那条查的是"语言包里的键有没有人提到"，所以**语言包里根本没有的键**它无从查起。于是
  实测：往 popup.js 加一句 `msg("noSuchKeyXyz")`，改前 `node scripts/validate.mjs` **exit 0**，
  同一处破坏在 A26 之后 exit 1 并点名 `popup.js: msg() 引用了语言包里没有的键 noSuchKeyXyz`。
  写成 `chrome.i18n.getMessage("noSuchKeyAbc")` 改前就是 exit 1——一个字符不差的两条写法，
  一边有门禁一边没有，差别只在有没有那层包装
- 已存在的键打错一个字母（`msg("btnStart")` → `msg("btnStartt")`）改前会红，但红的是
  "键 btnStart 在插件源码里没有一处提到"：那是**果**不是因，报账把协作者指向"这条文案是不是废弃了"，
  而不是"你拼错了"。现在两条同时红，正向那条直接点名拼错的键
- 症状不致命但静默：`msg()` 写着 `|| key` 兜底，所以界面露的是原始键名而不是空白，用户会当成本地化
  没做好，报上来也说不清是哪条文案。真正的代价是这种错能一路过 CI 进到 Release
- 别名名**从源码现推，不写死一张别名表**：判据是"谁把自己的入参原样递给 getMessage，谁就是别名"。
  写死表就是第二份真相来源——`function msg` 改天叫 `lbl`，或者第三个页面又抄一个包装，表不会跟着动，
  而"扫不到键"在这套判据里的表现是不报错。对照里真跑了改名那一处：popup.js 整词改成 `lbl` 之后
  同一句假键照样 exit 1，报的是 `lbl()`
- 两种写法各存在一处，不是顺手多支持：`popup.js` 是 `function msg(key, subs)`，
  `wechat-setup.js` 是 `const msg = (key) => ...`。对照里只废掉箭头那一支推导，教程页那句假键就
  exit 0 了——只认 function 声明等于把整个教程页留在射程外，而那正是最自然的写法会落到的地方
- 抽取器与报账分家（纪律 1 对工具脚本的又一处兑现）：`i18nAliases` 与加了第二参数的 `jsMessageKeys`
  住在 `scripts/validate-refs.mjs`，`validate.mjs` 只多一个循环把别名通道的键并进同一条判据。
  报账把别名名直接写进消息，不用回去猜是哪条通道报的
- 新增 7 条用例（`validate-refs.test.mjs` 12 → 19），"四条通道"那两节改成"五条"：两种包装形状、
  别名调用点复用同一条实参规则（三元分支收、变量不收）、`setMsg(` 与 `pushMsg(` 不算命中、
  "调用了 getMessage 但不算别名"（拿 background.js 里 `buildWechatContent` 那个真实形状当反例）、
  函数体切早/切晚不能把隔壁函数的调用算到头上、改名后判据跟着走。真实目录那两节多一条下限
  （别名键实测 48、钉 ≥40）和一条"别名面恰好是 `popup.js:msg` 与 `wechat-setup.js:msg`"
- 对照 10 处在仓库外整仓副本实跑（脚本 `D:/Github/_tar_ctl_r8/run.mjs`，每个变体跑两样：副本的
  `validate.mjs` 看 exit 码，副本的本文件看抽取器的正向见证）：base 两边都干净；popup.js / wechat-setup.js
  各加一句假键 exit 1 且本文件红 1；同一处破坏配**改前那两份 scripts** exit 0（就是上面那个洞）；
  只废箭头推导 + 教程页假键 exit 0 而本文件红 3；改名那处 exit 1 报 `lbl()`；打错已存在的键 exit 1
  两条，配改前只有一条果；最后两条是同一套打法测闸门自己——**废掉推导条件之后 `validate.mjs`
  exit 0，即使现场真有一个假键也还是 exit 0**，唯一拦得住的是本文件那两条数量下限（红 5 条）；
  最后一条 B6 改坏的不是产品代码而是**见证自己**——把下面那处写坏的过滤器原样装回副本，
  19 条照样全绿，用它确认那处账不是事后编的。
  逐条红名单记在 `validate-refs.test.mjs` 文件末尾
- 一处门禁自己的账：第一次写"别名面"那条见证时过滤器写成对空串求值，等于恒真、压根没过滤，
  副本跑出来照样全绿（B6 就是量这一句）——因为本仓库两个 HTML 没有内联脚本，推导在它们身上本来就
  回空集。判据恰好落在一份空洞语料上时，坏味道和绿长得一模一样，改成按文件名筛 JS 后才确认这条真有覆盖面
- 边界写在测试文件末尾：别名只在本文件内推导（跨文件 import 的包装看不见，本仓库现有两处都是
  同文件定义同文件调用）；函数体靠花括号配对切，跑偏刻意选成"多扫一段"而不是"少扫一段"，
  因为多扫不红、少扫是静默零覆盖；`obj.msg("x")` 那种同名方法调用会误收，表现是假报警；
  打错的键恰好也是语言包里的另一个键时两条判据都不红，那是静态检查的天花板
- 全套 480 → **487** 条全绿，`scripts/validate.mjs` 通过（40 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A27 同日结案（第九轮），审的还是那条文案链，但换了一根轴：A26 管"这个键在不在语言包里"，
这一轮管"这个键要填几个值、调用点填了几个"。产品代码一个字没改。

- **这一轮也没抓到一处真漂移**，先说清楚。人肉核之前先把两边量出来再对：语言包 18 个键带占位符
  （17 个一位、`skipChipTitle` 一位加一位共两位），具名引用与 `placeholders` 声明两边齐平、
  两份语言包位数一致；调用点 61 处能定到具体键，其中 19 处要填值，**位数全部对得上**。
  也就是说今天的文案是真的渲染得出来的。这一轮的产出是那道门：下一次改文案忘了改实参，
  表现从"没人发现"变成"CI 点名是哪个键、要几位、给了几位"
- 症状与 A26 那类不同，它不是"少一块"而是**多一块**：位数不够时 Chrome 不报错，把没填上的
  占位符原样吐进界面，用户看到的是 `$SITE$` 这六个字符；多填的那一个无处可去，文案缺了主语。
  这一类在"键在不在"的五条通道上全是绿的——键明明存在。所以它值的不是"补一条正则"，是补一根轴
- 判据分三段落，各配正反两面：`localePlaceholderFacts`（语言包侧）钉"消息里写了却没声明"与
  "声明了却没引用"两头，缺一头另一头就静默；`jsMessageCalls`（调用点侧）钉"数组数顶层逗号、
  空数组 0、变量与展开一律回判不了"；`validate.mjs` 把两边对上，另加**没有传参面的两条通道**
  （manifest 的 `__MSG_key__` 与 `data-i18n*`）用了要传参的键就点名——那两处的键都存在，
  五条通道一条都不红，位数是唯一认得它们的判据
- 与 `jsMessageKeys` 的分工写进了注释，因为两者对同一个实参的取舍**刻意相反**：那条要"像键的都
  收下来"去比语言包（多收只会在反向判据那里被当成提到过，不假报警），这条多收就是假报警——
  三元和变量都对不上具体键，没法比位数。写坏哪一头都有用例红
- 不 import 模块、不写第二份"这个键要几位"：位数从语言包现推，取声明里的最大位置号。
  大小写按 Chrome 的口径忽略（消息写 `$SITE$`、声明叫 `site` 是合法的），不忽略就会把 18 条
  真文案全报成违规
- 新增 9 条用例（`validate-refs.test.mjs` 19 → 28）：六位事实表（含大小写、裸位置号、两种坏法）、
  两头各自的红、调用点八种实参形状、三元不收、嵌套与点号直写都收、数组空洞占一位、
  "位数与键名共用同一套实参切分"（挡住本轮把 `firstArgs` 重构到 `callArgsList` 之上时切坏），
  再加真实目录四条：数量下限、位数比对、两份齐平、不传参通道
- 对照 17 处在仓库外整仓副本实跑（脚本 `D:/Github/_tar_ctl_r9/run.mjs`，每个变体跑两样：副本的
  `validate.mjs` 看 exit 码，副本的本文件看红名单；O* 那四组先把副本的 scripts 换回 0c3ecc7 那份）：
  base 两边干净；调用点少给一位 / 多给一位 / 直写通道删实参各红 1 条，**同两处破坏配改前 exit 0**；
  语言包删引用、加没声明的引用各红（配改前同样 exit 0，也就是这一整族改前一条判据都没有）；
  只往 en 加一个用不上的声明红在语言包那条、只把 en 的第二位改成 `$3$` 红在齐平那条而调用点那条
  不红——三条判据各管一段，不是同一判据的三种说法；manifest 与 `data-i18n` 各塞一个要传参的键
  各红 1 条；最后三处是量闸门自己：**把抽取器写成"实参数一律判不了"，`validate.mjs` exit 0**，
  现场一条都不报，唯一拦得住的是本文件那条"要传参的调用点 ≥ 15"的数量下限（红 3 条）——
  与 A26 的 B5 同一条道理，下限必须比判据本身更严
- 一处门禁自己的账：第一版探针把语言包的位数映射写成 `[k, f]`（整个事实对象）而不是 `[k, f.arity]`，
  于是"需要替换值"恒 0、"对不上"恒真，跑出来是"61 条全违规 + 一条都不需要替换值"——错得刺眼反而
  一眼看得见。真正危险的是它更早那一次输出：切实参时把左括号带进了第一个实参，字面键匹配整条没命中，
  报的是"零条违规、全部对得上"。**判据扫不到东西时的表现就是不报错**，这一条本轮又现场撞了一次
- 边界写在测试文件末尾：位数对但**顺序**错（第一位该填站点却填了时刻）判不了，两个实参都是字符串；
  实参写成变量或 `[...xs]` 展开一律判不了、不比对（本仓库目前零处，动静只在数量下限那条上）；
  "消息里裸写 `$1`、只靠声明给位置号"那一支真实仓库零处，覆盖面只有 fixture 担着（对照里现场
  exit 0、只红 fixture 那一条）；位数比对以默认语言包为基准，另一份单独改由"齐平"那条红
- 全套 487 → **496** 条全绿，`scripts/validate.mjs` 通过（40 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A28 同日结案（第十轮），换一根轴：前六轮审的都是"扫源码能不能看见"，这一轮审的是**量像素**——
`AGENTS.md` 里那条"整页高度必须留在 600px 内"从写下那天起只有一道检查：人看图。
产品代码一个字没改，量出来的结论是六个形状全在限内（最深 571px，预算 29px）。

- 症状不是报错也不是滚动条，是**直接裁掉**：`popup.css` 的 body 是 `max-height: 600px` 加
  `overflow: hidden`，超出 600 的部分不折叠、不出滚动条，就是看不见。所以验证清单里那句
  "弹窗本身不该出现滚动条"其实判不了什么（body 是 hidden，永远不会有自己的滚动条），
  要问的是"最深的一条底边落在哪"——本轮把这句话做成了可跑的判据
- 为什么此前只能人眼看：CI 上没有 Chrome，任何像素判定都进不了 `node --test`。而
  `scripts/screenshot-popup.mjs` 早就把弹窗真渲染了一遍（同一台 Chrome、同一份 CSS、同一套
  chrome mock），只是它只写图不报数。本轮给它加 `--measure`：同一套 mock、一个字节图都不写，
  逐形状打印最深底边并与 600 比，任一超出就 exit 1。**没有另起第二份 mock**——抄一份出来就是
  第二个真相来源，那份 mock 与弹窗用面的对齐本来就由 `screenshot-mock.test.mjs` 钉着
- 六个形状与实测（视口沿用截图那条 400×640：宽必须等于 `popup.css` 的 body width，高无所谓——
  深边是 `rect.bottom`，布局盒不被视口裁剪，本轮实测同一份内容在 600 与 640 下报同一个数）：
  主视图（两张任务 + 一行跳过解释）541px；填了合法 webhook 地址 571px；
  地址非法（只剩红字那一行）561px；微信直连开着 510px；微信概览行与 webhook 状态行同时出现 540px；
  微信二级视图 380px。最狠的一条是 `footer#repoFooter`，二级视图那条是 `div#wechatView`
- 形状怎么挑的有一条反直觉，记下来免得下次按"行数最多"去挑：两行状态同时出现那一种**比**只填
  webhook 那一种**矮** 31 像素——因为它的场景底座只有一张任务。所以"最挤"要看底座，不是数行数
- 门禁加 1 条（`screenshot-mock.test.mjs` 4 → 5）：量高度的形状必须引用真实存在的截图场景、
  补丁键必须是 `DEFAULT_SETTINGS` 里真有的一格，外加"扫到 ≥6 个形状、≥3 个补丁"的正向见证。
  为什么单独判补丁键：合并进去却没人读的键，表现是那一行根本没出现、而测量照样全绿——
  还是"判据扫不到东西就不报错"那一类
  - 同一次把"每个场景给全 `DEFAULT_SETTINGS` 的键"那条的扫描范围收进 SCENARIOS 那一段。
    合并式写法 `settings: Object.assign(...)` 今天碰巧扫不中（冒号后面不是 `{`），那是运气不是设计：
    换一次行它就红成"第 3 个场景的 settings 缺键"，把红指到一个不存在的场景上。收窄之后
    "给全键"只管截图场景，补丁是增量、合并进完整场景之后才要求齐备
- 对照 8 处在仓库外整仓副本实跑（脚本 `D:/Github/_tar_ctl_r10/run.mjs`）：base 5 条全绿；
  补丁键打错一个字母红 1；形状引用的场景不存在红 1；探针清单被清空红 1（这一处是真静默——
  脚本照样跑、照样全绿，只是什么都不量，只有正向见证认得）；从场景里删掉一个键照样红 1
  （老判据没被收窄弄坏）；两处反向绿：合并式改写成多行仍绿（证明收窄生效）、补丁换成另一个
  真实设置键仍绿（证明判据不是"改什么都红"）；最后一处是**运行时那一头**的对照——把上限常量
  压到 500px 再真渲染，`--measure` exit 1、六个形状五个 ✖ 一个 ✔（380px 那条还在限内），
  这一处是"✔ 全在 600 内"这句话不是空话的唯一证明
- 一处**没抓住**的改坏，记下来别当成对照通过：只把 `const PROBES` 改名、不改使用处，判据照绿，
  因为它数的是 `shot:` 字面量、不看接线。可接受的理由是脚本自己会在 `PROBES is not defined`
  上抛错，那是响亮地崩而不是静默量不到；真静默的那一类（清单清空）由正向见证兜着
- 本轮自己写错的一条口径：草稿把"视口给到 400×640"说成测量成立的前提（"视口只有 600 的话超出
  部分被视口自己切掉"）。那是没验的直觉。实测同一份内容在视口 600 与 640 下最深底边都报同一个数
  ——`getBoundingClientRect()` 是布局盒、不被视口裁剪，所以高度沿用截图本来的 640 就行，与测量无关
  （绑死的只有宽度要等于 `popup.css` 的 body width）。两份文档已改成实测的说法，脚本注释里也留了一句
- 边界：数字只在本地量得到，CI 上没有 Chrome，本仓库钉的是形状不是高度；任务数不逐一样量，
  `#taskList` 有 108px 封顶加内部滚动，多开任务不会把页面拉长，那是 CSS 保证的；量的是 mock
  数据下的形状，真系统的字体回退、用户 zoom、更长的中文站点名仍只能真机看——V1 (h) 因此收窄成
  那一半，不再包含"这一行会不会把页面顶破"
- 全套 496 → **497** 条全绿，`scripts/validate.mjs` 通过（40 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A29 同日结案（第十一轮），审的还是那条渲染通道，**插件产品代码一个字没改**。两根轴一起收：
弹窗按 id 取控件到底有没有对过账，以及那条通道遇到页面报错算不算失败。前一问的答案是"只对账了
一个函数"，后一问的答案是"不算"——于是上一条（A28）量到的那六个数字，出自一条页面死了也照样绿的通道。

- 失败形状是硬崩不是缺一行：`$()` 就是 `getElementById`，取不到回 null，紧跟的
  `.addEventListener` 抛在模块顶层，整个弹窗脚本当场停住。今天 `popup.js` 有 42 处字面量取值，
  旧门禁只点名了 6 处（`renderWebhook` 里那四处加 `popup-save-timing` 那份清单里的两处），
  剩下 36 处随改名、随误删都没人看着。新增的那条全量对账带"至少切出 40 个"的正向见证下限，
  方向刻意单向：HTML 里有 id 而 js 没取过不算错（`repoFooter`、`wechatView`、`wechatGuideLink`
  三条今天就是这样，分别由 CSS 与那条 target=_blank 的锚点用着），反过来才致命。
  按变量取的那两处不在判据里，仍由 `popup-save-timing.test.mjs` 按它自己那份清单钉
- 更要紧的是运行时那一头：`scripts/screenshot-popup.mjs` 是唯一能把弹窗真渲染一遍的通道，
  而它原先对 pageerror 与 console error 只做一个动作——`console.error` 打一行，退出码照 0、图照写。
  现在两类错误攒进同一份清单，逐条打印，末尾非空就 exit 1；截图与 `--measure` 两种模式共用这套收口，
  一次跑完把账报全，不在第一条错就中断。门禁 +2 条（`screenshot-mock.test.mjs` 5 → 7）钉的是：
  两条订阅都得往**同一个**清单里 push、console 那条仍要筛类型（不然弹窗一条普通 log 就能让整次运行红）、
  而且必须有一处判据真的读这份清单并改退出码
- 那条"跑完固定有一行 404"的已知噪音一并根治，做法值得记：想按文本放过它做不到——实测报错文本
  只有 "Failed to load resource: ... 404"，不含网址，与真错分不开。所以不改判据改服务器：
  对 /favicon.ico 直接回 204，噪音不再产生，剩下的每一条 404 都是"该在而不在"。
  判据里同时钉了一条"console 订阅里不许出现按文本放行的分支"，防的是将来又用 includes 开后门
- 对照 8 处（仓库外整仓副本，脚本 `D:/Github/_tar_ctl_r11/run.mjs`，基线 29 条全绿）：HTML 里
  一个 id 的属性名改掉红 1（红在全量那条，`renderWebhook` 那两条老 id 判据毫无反应——这就是增量）；
  报错改回"只打印"红 1；两类错误各攒一份红 1；读清单却不改退出码红 1；删掉 favicon 那一支红 1；
  加一段按文本放行的白名单红 1；反向两处全绿（清单整体改名 9 处仍绿 = 判据不绑名字，
  HTML 多一个没人取的 id 仍绿 = 判据单向）
- 另两处是**真渲染**的对照，因为源码形状判据本身证明不了运行时会红。一处把 HTML 里一个 id 改掉
  再跑 `--measure`：exit 1、pageerror 当场点名 "Cannot set properties of null"——但它红的是
  `waitForSelector` 超时（脚本死了任务列表根本没建起来），也就是"整页白屏"这一类本来就拦得住，
  本轮补的不是这一类。另一处删掉 favicon 那一支再跑：六个形状高度全部 ✔、最深仍是 571px，
  只有末尾"1 条页面报错"让它 exit 1。**这一处才是新判据独有的覆盖面**：页面照建、数字照量、
  看着一切正常，红只可能来自清单。两条对照分开摆是有意的，一条量增量一条量存量，
  别拿"本来就响"的那条当新机制的证明
- 顺带查实：`docs` 那两张图不是幂等产物。同一份产品源码、同一份脚本，隔一段时间再跑
  `popup-wechat.png` 的字节就变了（实测与 37aa498 里那张差 36 字节，而那次提交到现在
  `tab-auto-refresh/` 零提交），连跑两次则完全相同。成因没去查（那条倒计时与相对时刻按加载耗时
  现算，最可疑）。可用的结论是：判断"这张图该不该重新提交"看的是产品源码自上次生成以来有没有过
  提交，不是看 diff 里有没有图片——已写进 `AGENTS.md` 验证清单第 4 条
- 边界：新门禁钉的是"id 取不到"这一类硬崩，取到了却用错地方（把某个框的值写进另一个框）认不出；
  报错清单只在**本次进程**里有效，脚本中途自己抛会跳过末尾的统计，那种情况红是红、报的却是超时；
  整条通道仍只能本机跑，CI 上没有 Chrome，与截图、量高度同一档
- 全套 497 → **500** 条全绿，`scripts/validate.mjs` 通过（40 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A30 同日结案（第十二轮），换一根轴：权限这本账。上一轮审的是"渲染那条通道看不看得见错误"，这一轮审的是
"账本上那一行清单到底记的是什么"。**插件产品代码一个字没改**——今天四段账刚好对上，本轮做的事是把"对上"
变成能跑的判据。

- 这本账有四段：`AGENTS.md` 的权限清单 ↔ `manifest.json` ↔ 源码里的真实调用点 ↔ 共享桩件暴露的 chrome
  表面，外加 `host_permissions`。改前只有第一段与第二段之间有人核（`doc-numbers.test.mjs` 的「对账 35」，
  它的对照 D8 就是"往 permissions 加一项，只红那一条"）。名字对了不等于账对了
- 缺权限这个 bug 类的症状是静默的：新增一处 API 用法忘了配权限，真机上那个命名空间是 undefined，
  TypeError 被调用点的 `.catch(() => {})` 或外层 try 吞掉，用户看到的是"这块功能就是不生效"。测试里
  桩件什么都给——那份 chrome 对象是人手抄的，抄的是代码当时用到了什么，不是代码被允许用什么。
  **这句不是推理，是量出来的**：把 manifest 里的 power 删掉，连着 `AGENTS.md` 那行清单一起改掉（让名字
  层面的对账无话可说），改前的整套 500 条全绿。整个 bug 类在第十二轮之前没有一面门拦得住
- 新增 `tests/tab-auto-refresh/permission-map.test.mjs` 4 条：① 调用点要的权限必须给了；② 每一项声明的
  权限必须有一处真实调用点背书（白要那一类）；③ `host_permissions` 必须正好是 `<all_urls>`，由源码里
  两类"按任意网址干活"的形状背书（裸 fetch 3 处、executeScript 3 处当下限的正向见证）；④ 桩件的 chrome
  表面不许宽到 manifest 之外。④ 与 ① 是一对：不拦"先给桩件补一个 API 让用例跑通"这一步，缺权限就永远
  结构性不可测。今天源码用到 14 个命名空间，5 个不需要权限（action / commands / i18n / runtime /
  windows），剩下 9 个需要，与 manifest 那 9 项一一对应——钉的就是"无多无少"四个字
- 表 `API_PERMISSION` 是平台事实（哪个 API 要哪一项权限，抄自 Chrome 文档），刻意不与 manifest 互为副本，
  否则就是第二个真相来源。表外的命名空间一律红：新用一个 API 必须先来查权限、加表行，而不是改完 manifest
  就完事。注释里的字样不算证据——反向那条一旦被注释满足，"功能删了、注释留着"就是永久白要一项权限，
  所以判据只认整行代码；这一处两头都验过（只有注释 → 全绿，注释 + 声明 → 红在白要那条）
- 判据单向：桩件比代码用得**窄**不判。实跑删掉桩件的 commands 命名空间 → 整套 189 条红，全是 import 期
  TypeError。响的那一面不需要门禁，这一句是给"为什么只写了一个方向"留的账
- 对照 13 处（仓库外整仓副本，脚本 `D:/Github/_tar_ctl_r12/run.mjs`，基线副本内本文件 4 条全绿、整套 504
  全绿）：删 power 权限红 2（①④ 同时红不是冗余——④ 正是 ① 长期看不见的原因）；同一处改坏删掉新门禁红 1
  （只剩名字那条）；连文档一起改掉红 0（**盲区的那条证据**）；带上新门禁再跑同一处改坏红 2（名字层面闭嘴
  也拦不住调用点这头）；加一项没人用的权限红 2（②与对账 35，一条管名字一条管调用点）；桩件多一个 API 红 1；
  源码筛法切空红 3（三条下限全响，"扫不到东西"在这里必须是红不是绿）；host 权限删掉红 1、收窄成按站点清单
  红 1；表外新 API 红 1；反向一处全绿
- 边界：表里那些"源码还没用到"的行是按文档抄的，本轮只逐条核过真用到的那 14 行，抄错一行的表现是那项 API
  一旦用上就假绿或假红、它不会自己承认；host 那条只认两类形状，将来出现第三种需要 host 权限的调用点
  （如带 url 过滤的 tabs.query）时反向那半会假红一次，那时该重看的是这项授权本身而不是调低下限；
  钉的是权限给了没有，不钉 API 的版本门槛（30 秒 alarms 依赖 `minimum_chrome_version` 那一类事实仍只在
  文档里）；权限变化与 CHANGELOG/README 的成对记账（纪律 2）仍然只有名字那头的对账是机器判的
- 全套 500 → **504** 条全绿，`scripts/validate.mjs` 通过（40 → 41 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A31 同日结案（第十三轮），换一本账：设置。上一轮审的是"权限那行清单到底记了什么"，这一轮审的是
"一个设置项要改齐几处、少改一处会怎样"。**插件产品代码一个字没改**——今天五段账刚好对上，本轮做的事
还是把"对上"变成能跑的判据。

- 这本账有五段：`DEFAULT_SETTINGS` 的键 ↔ 弹窗 `saveSettings` 发出的载荷键 ↔ `popup.html` 里能填值的
  控件 ↔ 插件源码里的读写点 ↔ `NOTIFY_EVENTS` 与弹窗那几个事件格子。改前只有两头有人核：
  `popup-settings-sync.test.mjs` 钉"保存读的控件＝回流铺的控件"（A18），`popup-repopulate.test.mjs`
  钉"`popup.js` 取的 id 在 HTML 里存在"（A29）。中间三段——键名对不对得上、格子有没有人取、
  取值方式对不对得上控件类型、事件名齐不齐——一个判据都没有
- 五段各自的静默形状，最狠的一条是**点了不保存**：载荷里多出一个 defaults 没登记的键，
  `save-settings` 进后台第一站就是 `pickKnownSettings`，未知键整个丢弃。弹窗控件当场已经改好、
  下一笔读盘又回到默认值，后台一个字节都不报错。**这句不是推理，是量出来的**：把 `keepAlive`
  那一项在载荷里改个名（键数仍是 15，所以 A18 那道计数守卫也不响）、并删掉本轮新文件，
  改前的整套 504 条全绿。同类的还有三处：defaults 里长出一个没人读也没人写的键（跟着 `sync`
  漫游、占配额、改不掉任何行为）；HTML 里摆一格 `popup.js` 从不取的复选框（一个从来没能生效过的开关）；
  `.checked` 与 `.value` 用错控件类型（前者存进去是 undefined，序列化时整个键消失；后者拿到 "on"，
  是一个永远真值的开关）——三处各自删掉本轮新文件也都是 504 全绿
- 新增 `tests/tab-auto-refresh/settings-map.test.mjs` 6 条：① 载荷每个键要在 `DEFAULT_SETTINGS` 里
  登记过；②③ 每个设置键在 `config.js` 之外既要有读取点也要有写入点；④ HTML 里每个能填值的控件要被
  `popup.js` 至少取一次；⑤ 取值方式要和控件类型对得上；⑥ 事件名在五处对齐（清单、格子、真跑出的载荷、
  `notifyOut` 调用点、格子类型）。另有一条空跑守卫：载荷键数、默认键数、控件数、`$()` 用法数、
  两张控件表、源文件数各有下限——"扫不到东西"在这里必须是红不是绿。今天 16 个默认键、载荷 15 个
  （差的 `lastIntervalSec` 由后台 `patchSettings` 写）、22 个能填值的控件全有人取、9 个用 `.checked`
  读的全是复选框、四处事件名对齐，钉的就是"对得上"
- ①⑤⑥ 跑真实源码：把 `saveSettings` 整段切出来、注入假 `$` 与假 `send` 真跑一次，载荷的键与事件数组
  是求值求出来的。正则数出来的是"源码长什么样"，求值求出来的才是"发出去的是什么"，这两处会分叉。
  ②③④ 只能扫源码，扫法与 A30 同规：注释行不算证据（`popup.js` 的注释里满是设置键名，拿它们当读点
  等于"功能删了、注释留着"也算有人在用）
- ⑥ 的"有没有出口"这一头钉的是 `background.js` 里必须有一处 `notifyOut("事件名")` 调用点，反向也钉。
  **这一条第一次写错、被对照抓出来**：原先拿"字符串在 `config.js` 之外还出现在哪个文件"当证据，
  而 `NOTIFY_EVENTS` 的声明自己就含那几个字面量——判据在自证，T6c 跑出来是绿的。换成调用点判据之后
  T6c 才红。这与 A30 记的"桩件抄的是代码用法、不是代码权限"是同一类错：出现在别处不等于被用
- 对照 18 处（仓库外整仓副本，脚本 `D:/Github/_tar_ctl_r13/run.mjs`，基线副本内本文件 6 条全绿、
  整套 510 全绿）：载荷新增键红 1、改名红 2；defaults 长出死键红 1；多一格没人取的复选框红 1；
  复选框换成文本框红 1、`.value` 读复选框红 1；事件清单加第五项红 1、格子名拼错一个字母红 1、
  五处只漏后台调用点红 1、后台发清单外的名字红 1；下限被改坏红 2。反向两处都绿：HTML 里多一个
  不填值的 div（④ 只管 input 与 select）、五处一起自洽地加第五个事件（判据钉的是"对齐"，
  不是"四项"这个数）
- 一处不理想的对照要老实记下：defaults 里长出死键那一类，改前的世界并非全盲——删掉本轮新文件、
  连 `AGENTS.md` 的默认关清单一起改掉，整套仍红 1 条，红在 `screenshot-mock.test.mjs` 的「每个场景
  给全 `DEFAULT_SETTINGS` 的每一个键」。那面门钉的是"场景清单齐不齐"，把新键补进那份清单它就闭嘴，
  "这个键没人读也没人写"它始终看不见。而新增键那一头（T1）旧世界也红 1 条，红的是键数从 15 变 16——
  计数守卫不等于对账，把新增换成改名（T1c）它就彻底闭嘴
- 边界：写点认对象字面量的 `key:` 与赋值 `key =`，计算键（`s[k] = ...`）不算证据，将来那种写法出现时
  ③ 会假红一次（响的那一侧，不会假绿）；读点同理不认 `settings["key"]` 这种字符串下标；④ 只管
  input 与 select，按钮有没有绑事件、格子摆在版面上合不合理是另外的账；本文件钉"键与控件对不对得上"，
  不钉值的形状（读侧 `normalizeStoredSettings` 归 `logic.test.mjs`）；新增设置项要一次改齐五处这件事
  现在写进 `AGENTS.md` 的纪律 3，但"改了默认值会不会改变既有用户的行为"（纪律 3 的正身）仍然是人判
- 全套 504 → **510** 条全绿，`scripts/validate.mjs` 通过（41 → 42 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

A32 同日结案（第十四轮），换一本账：消息。上一轮问的是"一个设置项要改齐几处"，这一轮问的是
"一条消息要对齐谁"。一条消息的名字分三段，每段都是**同一个字面量在两个文件里各写一遍**：
① 类型名（弹窗与页面发出的 `type` ↔ 背景分发链上的分支字符串）、② 请求载荷键（`send` 出去的键 ↔
分支里读的 msg 属性）、③ 应答字段（`sendResponse` 回的对象 ↔ 弹窗读的 res 属性）。
**插件产品代码一个字没改**——今天三段的名字刚好对上（分发口 11 条分支、弹窗 10 处 send 共 9 种类型、
页面侧 2 种、后台往页面推 3 处、`applyConfig` 读 2 个字段、弹窗读四个字段），本轮做的是把"对得上"
变成一条能跑的判据。

- 这一面为什么整片空白：`AGENTS.md` 早就记了两张表（`FROM_PAGE_TYPES` 与 `POPUP_ONLY`），
  可那两张表管的是**谁能发**，不是**报文长什么样**。类型名与载荷键全跨文件、全没有 import、
  全没有共用常量（`shared/config.js` 里有 alarm 前缀，没有消息类型名），所以改一边另一边
  连一次报警都不会有。它的症状永远是"点了没反应"：类型名打错落到分发链末尾那条 else，
  回一个 `{ok:false}`，而 `prune-now` 与 `resume-task` 两个调用点把返回值甩掉不看，
  用户连"失败"都看不到；载荷键打错那边读到 `undefined`，而 `undefined` 在 `keepWatching`、
  `keyword` 这几个参数上全是合法输入——"继续盯守"当成没勾、关键词当成没填。
  没有一条路径抛错，也没有一行日志会说这里少了一个字段
- 实测这台机器是瞎的，而且是从**两头**都瞎：从弹窗那一头删掉 `keepWatching`、把 `resume-task`
  打成 `resume`、删掉整条 `prune-now`、把 `res.intervalSec` 读成一个不存在的字段、把 `tabId`
  打成 `tab`——五样叠起来改前的 510 条一条不红；从背景那一头把 `msg.keepWatching` 改名，
  全套照样全绿。后一条值得单独记：测试侧唯一发过 `keepWatching: true` 的是 `detect-chain`，
  而它发完只看关键词与 `notifiedKeys`，从没回头看任务的 `onHit`。那一份是**第三份抄本**——
  两份真源码之间的账没人对，抄本自己多齐都对不上任何东西。"start 这条消息有测试覆盖"是错觉，
  这也是 ② 正向只认 `popup.js` 与 `keepalive.js`、不认测试侧的原因
- 新增 `tests/tab-auto-refresh/message-ledger.test.mjs` 8 条：空跑守卫（分支数、三处发送侧的调用点数、
  页面消费端各有下限，"扫不到东西"必须是红）、① 类型名两头齐平（双向）、② 载荷键正向、② 反向
  （发出去的每个键都有分支读它，多一个就是白占报文）、③ 应答字段、每条分支至少应答一次、
  页面配置三方齐平（推送载荷 == `keepalive-query` 应答 == `applyConfig` 读的字段）、
  `FROM_PAGE_TYPES` 的反方向（表里留一条没人发的类型，等于守卫的覆盖面比真实通道大，看着安全其实虚报）
- 判据形状选的是**只读源码、不执行任何函数体**：按花括号配对把真实源码里的对象字面量切出来求键名。
  这与十三轮相反（那里 ①⑤⑥ 真跑 `saveSettings`，因为要核的是"求值求出来的载荷"）。这里不能跑：
  跑起来就得先造一份假载荷，而造出来就等于把要核对的字面量重写第三遍——正是本文件要消灭的东西。
  注释行整行抹成空行而不是删掉，所以报错给的行号还能对得上原文；注释里出现的类型名与键名一律不算证据
- 两处**第一次写错、被实跑拦下来**：一是原本钉"每次 `sendResponse` 都带 `ok`"，切完源码才发现
  `keepalive-query` 刻意只回两个配置字段，页面侧靠 `if (resp)` 判有没有拿到，那半个断言删了。
  二是原本想钉 ③ 的反方向（分支不许多回一个没人读的字段），实跑当场红在 `toggle-pause-all` 那个
  没人读的 `pausedAll` 上——多回一个字段零代价（不进弹窗的载荷、不占 `sync` 配额），
  而为了凑绿去写一张豁免清单，等于把判据降级成手抄清单。两条都是"判据写得比事实严"，
  跟 A31 那条"出现在别处不等于被用"是不同类的错，各记一笔
- 对照 16 处，仓库外整仓副本实跑（脚本 `D:/Github/_tar_ctl_r14/run14b.mjs`）。本轮把每处改坏**跑两遍**：
  pre 删掉本轮新文件（等于本轮之前的世界），post 留着，所以"改前看不看得见"是量出来的而不是推出来的。
  基线 pristine：pre 510 全绿、post 518 全绿。pre 全盲的六条（V1~V6）改后分别红 1、2、2、1、2、2，
  这六条才是本文件的理由；其余九条改前就有人拦（2→4、1→2、7→8、3→4、4→6、2→整文件红、3→4、1→3、3→5），
  留作 overlap 并逐条记清是哪面门先响。两处方法上的收获：V9 那 7 条里只有 2 条真是名字账，
  另外 5 条来自 `settings-cache` 把 `keepalive-query` 的应答当成观察设置快照的只读窗口，
  一个字段名被四扇门同时按字面用着——这更说明它该进名字账，而不是靠"别人顺带撞红"；
  V12 红在**整个文件**而不是某个用例，因为三处模块级前置断言抛在 test 之外，切片的前提塌了就该这样响。
  另有一条反向自检（V16）：把空跑守卫的分支下限改成不可能的数 → 红 1，证明"切到了真东西"不是白写的
- 边界三处：测试侧那份第三抄本不入账（测试里故意喂一个假类型给末尾 else 是合法输入，拿真源码的口径
  要求它一定假红；② 正向反过来把测试抄本顶掉了，所以"测试发了、真弹窗没发"这种假覆盖照样红）；
  ③ 反向刻意不钉，理由记在上面那条；键名只认根级，嵌套对象里的同名（`save-settings` 那 15 个设置键）
  与计算键都不进账，所以载荷再往里套一层"名字对不上"，要靠 ② ③ 各跑一次才撞得出来。
  ② 反向钉的是"没有白占的键"，钉不了"值的形状对不对"（那是读侧 `normalizeStoredSettings` 与
  `normalizeWebhookUrl` 的账，归 `logic.test.mjs`）
- 全套 510 → **518** 条全绿，`scripts/validate.mjs` 通过（42 → 43 个 JS 文件）；默认值、权限、存储键、
  消息类型、插件源码、语言包一个都没动

## 工程账（E）

E1 早已修完移进 `CHANGELOG.md`，E2 是待定项（只有上面那张表里的一行，没有单独小节）。
下面这段记的是 E3 的结案，本节目前没有待做的工程账。

E3 同日结案：桩件补齐了两块记录面，两条链第一次有了自己的门禁，**产品代码一个字没改**。

- `chrome.power` 从两个空函数改成按序列记 `env.calls.keepAwake`。新增 `keep-awake.test.mjs` 7 条，
  钉住 `applyKeepAwake` 的三条判据（申请 `system` 不申请 `display`、持锁标记走会话态所以一个生命周期
  最多申请一次、释放那一头无条件）加两条入口（任务增删经 `updateBadge`、开关真变化经 `reconcileKeepAlive`）。
  此前 `applyKeepAwake` 在所有门禁里一次也没执行过，`AGENTS.md` 那句"power 锁的收敛挂在 `updateBadge`"
  只是读代码相信
- `fetch` 桩现在 await Promise 型应答、认 `init.signal`，未决的请求列在 `env.pendingFetch()` 上、
  句柄 `.abort()` 等价于后台那笔 15 秒计时器到点；另外导出带上限的 `settles()`。心跳与两个外发出口
  各加 2 条，把"发出去了但永远不回来"这一支从不可表示变成有判据。A15 那文件每条用例自己覆写
  `globalThis.fetch` 的局部绕法同时收回桩件，该文件由约 15 秒降到约 1.6 秒
- "哪几条门禁因此从空跑变成真跑"这一句按 E3 自己的要求答在这里：真正新跑起来的是 `applyKeepAwake`
  整段（0 → 7 条）与三条外发链路的超时支路（0 → 4 条）。既有用例里被这面门解开的只有一处：
  `heartbeat` 与 `outbound` 各自那句 `assert.ok(init.signal, ...)` 此前钉的是"控制器存在"，
  现在它背后那支真的会 abort——但这两条自己没变红变绿的必要，所以真正的见证由新加的 4 条提供。
  另有一处**假绿**顺带量出来并写进文件末尾，没去改它：`outbound` 的"网络被拒记 network"在
  桩件完全没有 fetch 时照样绿，因为 ReferenceError 恰好也被 catch 归成同一个桶
- 对照 10 处（改桩件 5 + 改产品 3 + 反向 2），两处第一轮零红/漏红都留着写进脚注：退回空函数最初
  只红 6/7（漏的那条断言的全是"没有"，补了 release 落点才 7/7）；句柄 `abort()` 改空最初**红 0**，
  只把文件时长从 0.7 秒拖到 30.3 秒，因为后台自己的 15 秒计时器照样会兜住——改成 `settles()` 之后
  才红 3 条。这条已抽成 `AGENTS.md` 测试纪律里的一条：桩件坏掉的一面门若表现为"慢"而不是"红"，
  用例的等待就得自己带上限
- 全套 394 → **405** 条全绿，`scripts/validate.mjs` 通过；默认值、权限、存储键、消息类型一个都没动

## 只能真机验（V1）

在 `chrome://extensions` 加载插件目录，按 `AGENTS.md` 验证清单第 5 条走一遍，本批新增 (c)~(k) 九条
（(k) 是 A16 结案时补的）：

- (c) 开着"尊重你的操作"，把某任务页设为所在窗口的活动页，然后把焦点切到别的应用，等 2~3 个刷新
  周期——它**必须照常刷新**（对应 A1）。
- (d) 给某页开一个带关键词的任务，关掉弹窗再打开，关键词框与"命中后继续盯守"要显示该任务的实际值；
  直接点"开始"（先停后起）不能把关键词弄丢。A5 已修，判据与调用顺序由 `popup-repopulate.test.mjs` 钉住，
  但"控件真的被填上了"这一步切片用例给不了。
- (e) 在 webhook 框里打完字，不按 Tab、不点别处，直接点弹窗外关闭；重开弹窗内容要还在（对应 A11）。
  A11 之后这条从"必然丢"变成"多半救得回来"：`input` 去抖 500ms 已经写了一笔，关窗时 `pagehide` /
  `visibilitychange → hidden` 各补一次，而那两笔发生在文档正在销毁的路上，`sendMessage` 不保证赶得上。
  所以要验的是**残留窗口**：打完立刻点外（不到 0.5 秒）重开在不在，停一下再点外在不在。
  判据与"没有待写就不白写"由 `popup-save-timing.test.mjs` 钉住，"这一笔赶不赶得上"只有真机说得清。
  顺带两个五秒的检查，都属同一条：非法地址一边打一边出红字（修前要等失焦，等于从没出现过）；
  凭据填缺时点微信「发送测试」，报错之后按钮要能退回"发送测试"，而不是永远停在"发送中"
- (f) 多框架检测（对应 A3）：找一个整页挑战嵌在 iframe 里的站点（顶层只剩空壳标题那种），
  开任务后等 3 个加载周期，要被自动暂停、角标变 `⚠`；反过来，页面上只有一个验证挂件
  （reCAPTCHA / hCaptcha 那种小框）的正常页面**不该**被暂停。切片用例给不了这两条，
  它要的框架尺寸与自身网址都得是真实 iframe 才作数
- (g) 给一个**前端哈希路由**的页面（网址长这样：`https://x.test/#/orders/42?tab=1`）开任务并收到
  外发通知，点开微信卡片或 webhook 里的链接：A4 把 query 与 hash 一起剪掉了，落点会是
  `https://x.test/` 即应用根，而不是 `#/orders/42` 那一页。剪枝的收益（一次性令牌不出机器）
  由 `outbound.test.mjs` 钉住了，"落点还是不是用户要找的那一页"只有真机说得清。
  要验的是这件事对日常使用到底碍不碍事——不碍事就维持现状，碍事再谈怎么在不回吐 query
  的前提下把深链带上（记在 `CHANGELOG.md` 的 A4 那条里）
- (h) 在 webhook 框里填一个合法地址，弹窗要多出"发送测试 + 状态"那一行（A8）。要看的是**多出来这一行
  会不会把页面顶破**——这一半已经就地量掉了（A28）：`node scripts/screenshot-popup.mjs --measure`
  把弹窗按六个形状各真渲染一遍（同一台 Chrome、同一份 CSS、同一套 chrome mock），最深底边
  541 / 571 / 561 / 510 / 540 / 380px，全在 600px 内，最紧的一种是"填了合法 webhook 地址"，
  预算 29px。A10 之后要看第二遍的那一种（微信配置二级视图，`wechatIntro` 中英都改长过）在同一批里，
  量到 380px。`popup-repopulate.test.mjs` 钉的是显隐判据与控件齐备，这一半钉的是像素。
  剩给真机的只有量不到的三个变量：系统字体回退、浏览器 zoom、更长的中文站点名会不会把哪一行挤出换行
  （`.row` 与 `.wx-state` 都写着单行加 ellipsis，理论上不会，但那仍是读 CSS 相信的）
- (i) 中文冒号到底是必需还是仅仅充分（对应 A10）。本仓库从没实测过：依据只有 2.0.0 当时的调查记录
  加官方文档示例的写法。A10 的处理是不让教程为一个未证的备选写法留"换成英文冒号再试"这种兜底——
  照它做等于把唯一合规的写法改掉。真要结案得有微信测试号：按「标题：{{title.DATA}}」「内容：{{content.DATA}}」
  两行建模板，只把中文冒号换成英文 `:` 再发一次，看卡片是空白还是照旧渲染
  - 英文冒号也认 → `guideStep2Why` 的"微信只认「关键词：{{变量}}」"说过头了，改成"要有关键词加冒号，
    中英两种都收"，同时把 `wechat-copy.test.mjs` 的"只教一种冒号"放宽
  - 英文冒号不认 → 现状即正确，这条从 `BACKLOG.md` 删掉
  那条冒号守卫钉的是"教程口径唯一"，钉不了平台行为，别把它当成已验
- (j) 跳过痕迹与备份索引迁移（对应 A12）。两半都是切片用例给不了的现场：
  ① 开着"尊重你的操作"，把某任务页摆在当前窗口前台等它到点，任务行末要出现"· 上次跳过：你在操作"，
    鼠标停上去要带那一刻的时间；再分别看全局暂停与自动暂停这两种情形，这一行**不该**出现
    （行内已有角标与状态文字）。顺带看一眼弹窗仍不出滚动条——窄屏字体放大时最容易顶破
  ② 从 2.1.0 升上来（或在 `chrome.storage.local` 里手动留一个 2.1.0 那版写过的 `cookieBackupHosts` 键）
    且备份开关开着：重启扩展之后各站点原有备份必须**原样还在**、重启后恢复登录照旧生效，
    而那个遗留键不参与任何判断、也不算存档（索引本身已被 A13 回退：备份按构造可枚举）。
    这一条错起来的表现是"重启后不再恢复登录"，是丢数据不是变慢，而升级路径在真机上只走一次，值得亲眼看一遍
- (k) 恢复进行中就点弹窗（对应 A16）。开 2~3 个任务、重启浏览器、趁标签页还在加载就点一次弹窗：
  任务一条都不许少（这一条桩件给不了真实时序，`prune` 那 1500 毫秒与真实的会话恢复不是同一个时间尺度）。
  反向的一半也只能真机看：确有一张被监控的页在这个会话里不会回来了（例如上个会话末尾关掉的窗口里的任务），
  等恢复收尾之后再开一次弹窗，要看到一条"已停止"的系统通知，而不是什么都不说就少一条任务

既有缺口不变：开几个任务后重启浏览器或重新加载扩展，任务要全挂回原页面、不多开标签页、弹窗不卡住，
且 DevTools 背景页 Alarms 里 `refresh-<id>` 与 `hb-<id>` 同时存在（缺 `hb-` 说明心跳又被排到写盘前面）。

## 审计中推翻的结论（留档，避免被重新报一遍）

- "三处 `executeScript` 落在 MAIN world，保活脚本的 `chrome.runtime` 通道断了，页面可伪造活动"
  —— 假。`chrome.scripting.executeScript` 默认 `world: "ISOLATED"`，`content/keepalive.js` 的
  守卫与消息通道都在隔离世界正常可用。真实存在的只是相邻事实：合成事件带 `isTrusted: false`，
  站点看得见我们注入过。
- "`logic.test.mjs` 的抖动范围断言 `hi <= 60000 * 1.5 + 1 && hi >= 60000` 是恒真断言"
  —— 假。去掉 `pct` 上限夹取后 `hi` 会变 114000，
  这条会红，夹取实际是被钉住的。它只是范围宽，不是空跑。
- "`alarm-gate` 的焦点用例没做对照" —— 假，而且做过了（文件末尾第 4、5 处）。问题不在断言松，
  在桩件把 `getLastFocused` 建模反了。对应条目已修完移进 `CHANGELOG.md` 的 `[未发布]`。
- A19 那批探针顺手排掉的三条（同一份弹窗侧报告报回来的，逐条在本仓库重跑过）：
  `sortTasks` 这个函数**根本不存在**；"`getTaskKeywords` 丢掉旧单串 `keyword` 的兼容读" → 红 2 条，
  有门禁；"`looksLikeLoginPage` 恒返回 false" → 红 4 条，其中一条是 `outbound.test.mjs` 的
  "两个出口一共只发出 0 笔，下面的断言会空跑"。三条都不是账。
